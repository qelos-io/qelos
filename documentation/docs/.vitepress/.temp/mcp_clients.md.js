import { resolveComponent, useSSRContext } from "vue";
import { ssrRenderAttrs, ssrRenderStyle, ssrRenderComponent } from "vue/server-renderer";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"MCP Client Setup","description":"","frontmatter":{"title":"MCP Client Setup","editLink":true},"headers":[],"relativePath":"mcp/clients.md","filePath":"mcp/clients.md","lastUpdated":1785182671000}');
const _sfc_main = { name: "mcp/clients.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  const _component_CopyOrDownloadAsMarkdownButtons = resolveComponent("CopyOrDownloadAsMarkdownButtons");
  _push(`<div${ssrRenderAttrs(_attrs)}><div style="${ssrRenderStyle({ "display": "none" })}" hidden="true" aria-hidden="true">Are you an LLM? You can read better optimized documentation at /mcp/clients.md for this page in Markdown format</div><h1 id="mcp-client-setup" tabindex="-1">MCP Client Setup <a class="header-anchor" href="#mcp-client-setup" aria-label="Permalink to “MCP Client Setup”">​</a></h1>`);
  _push(ssrRenderComponent(_component_CopyOrDownloadAsMarkdownButtons, null, null, _parent));
  _push(`<p>Connect external AI clients to your Qelos tenant as a <strong>remote MCP server</strong>. Each client uses OAuth: when you connect, the client opens a browser window to your tenant&#39;s consent screen — sign in, review the request, and click <strong>Accept</strong> to grant access.</p><p>Before connecting any client, complete the admin prerequisites in <a href="./configuration">Configuration</a>:</p><ul><li>[ ] MCP server <strong>enabled</strong></li><li>[ ] Client <strong>callback URL</strong> on the permitted list</li><li>[ ] Desired <strong>tool categories</strong> enabled for your role — see <a href="./tools">Permitted tools</a></li></ul><h2 id="server-url" tabindex="-1">Server URL <a class="header-anchor" href="#server-url" aria-label="Permalink to “Server URL”">​</a></h2><p>Every supported client connects to the same tenant endpoint:</p><div dir="ltr"><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark" style="${ssrRenderStyle({ "--shiki-light": "#24292e", "--shiki-dark": "#e1e4e8", "--shiki-light-bg": "#fff", "--shiki-dark-bg": "#24292e" })}" tabindex="0" dir="ltr"><code><span class="line"><span>https://&lt;your-tenant-domain&gt;/api/mcp/admin</span></span></code></pre></div></div><p>Replace <code>&lt;your-tenant-domain&gt;</code> with your Qelos tenant host (for example <code>acme.qelos.app</code>). The URL is the same for all clients — only the OAuth callback URI differs per client.</p><h2 id="cursor" tabindex="-1">Cursor <a class="header-anchor" href="#cursor" aria-label="Permalink to “Cursor”">​</a></h2><p><a href="https://cursor.com" target="_blank" rel="noreferrer">Cursor</a> supports remote MCP servers with browser-based OAuth.</p><h3 id="_1-permit-the-callback-url" tabindex="-1">1. Permit the callback URL <a class="header-anchor" href="#_1-permit-the-callback-url" aria-label="Permalink to “1. Permit the callback URL”">​</a></h3><p>In <strong>Privileged Home → MCP Configuration</strong>, add:</p><div dir="ltr"><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark" style="${ssrRenderStyle({ "--shiki-light": "#24292e", "--shiki-dark": "#e1e4e8", "--shiki-light-bg": "#fff", "--shiki-dark-bg": "#24292e" })}" tabindex="0" dir="ltr"><code><span class="line"><span>cursor://anysphere.cursor-mcp/oauth/callback</span></span></code></pre></div></div><p>This is Cursor&#39;s fixed redirect URI. It must match exactly.</p><h3 id="_2-add-the-remote-server" tabindex="-1">2. Add the remote server <a class="header-anchor" href="#_2-add-the-remote-server" aria-label="Permalink to “2. Add the remote server”">​</a></h3><p><strong>Option A — config file (recommended for teams)</strong></p><p>Create or edit <code>.cursor/mcp.json</code> in your project root (or <code>~/.cursor/mcp.json</code> for a global setup):</p><div dir="ltr"><div class="language-json"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark" style="${ssrRenderStyle({ "--shiki-light": "#24292e", "--shiki-dark": "#e1e4e8", "--shiki-light-bg": "#fff", "--shiki-dark-bg": "#24292e" })}" tabindex="0" dir="ltr"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">{</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">  &quot;mcpServers&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">    &quot;qelos&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">      &quot;url&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;https://&lt;your-tenant-domain&gt;/api/mcp/admin&quot;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    }</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  }</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre></div></div><p><strong>Option B — Settings UI</strong></p><ol><li>Open <strong>Cursor Settings</strong> → <strong>Tools &amp; MCP</strong> (or <strong>Settings → MCP</strong>).</li><li>Click <strong>Add server</strong> / <strong>New MCP server</strong>.</li><li>Choose a remote server and enter the URL above.</li></ol><h3 id="_3-authenticate" tabindex="-1">3. Authenticate <a class="header-anchor" href="#_3-authenticate" aria-label="Permalink to “3. Authenticate”">​</a></h3><ol><li>Save the config or add the server in Settings.</li><li>If the server shows <strong>Needs authentication</strong>, click <strong>Connect</strong> (or trigger a Qelos tool from chat).</li><li>Your browser opens the Qelos consent screen at <code>/mcp/authorize</code>.</li><li>Sign in with an admin account (when <a href="./configuration#admin-only-access">admin-only access</a> is enabled).</li><li>Review the client name, callback URL, and scopes — click <strong>Accept</strong>.</li><li>The browser redirects to <code>cursor://…</code> and Cursor completes the OAuth handshake.</li></ol><p>After success, Qelos tools appear under the <code>qelos</code> server in <strong>Tools &amp; MCP</strong>.</p><div class="tip custom-block"><p class="custom-block-title">Project vs global config</p><p>Use <code>.cursor/mcp.json</code> in a repo to share the server URL with your team. Each developer still completes their own browser consent on first connect.</p></div><h2 id="claude-claude-ai-claude-code-cowork" tabindex="-1">Claude (Claude.ai, Claude Code, Cowork) <a class="header-anchor" href="#claude-claude-ai-claude-code-cowork" aria-label="Permalink to “Claude (Claude.ai, Claude Code, Cowork)”">​</a></h2><p>Anthropic clients connect to remote MCP servers through custom connectors. The redirect URI must be on your tenant&#39;s permitted list.</p><h3 id="_1-permit-the-callback-url-1" tabindex="-1">1. Permit the callback URL <a class="header-anchor" href="#_1-permit-the-callback-url-1" aria-label="Permalink to “1. Permit the callback URL”">​</a></h3><p>Add the redirect URI your Claude product uses. The most common pattern:</p><div dir="ltr"><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark" style="${ssrRenderStyle({ "--shiki-light": "#24292e", "--shiki-dark": "#e1e4e8", "--shiki-light-bg": "#fff", "--shiki-dark-bg": "#24292e" })}" tabindex="0" dir="ltr"><code><span class="line"><span>https://claude.ai/api/mcp/auth_callback</span></span></code></pre></div></div><p>Other URLs on <code>claude.ai</code> or <code>*.anthropic.com</code> are also recognized by the admin form&#39;s client hint. Add the <strong>exact</strong> URI shown in your connector settings — character for character.</p><table tabindex="0"><thead><tr><th>Product</th><th>Typical setup location</th></tr></thead><tbody><tr><td><strong>Claude.ai</strong> (web)</td><td>Settings → Connectors → Add custom connector / remote MCP</td></tr><tr><td><strong>Claude Code</strong></td><td>MCP or integrations settings — add remote server URL</td></tr><tr><td><strong>Cowork</strong></td><td>Workspace integrations — add remote MCP connector</td></tr></tbody></table><p>UI labels vary by release; look for <strong>custom connector</strong>, <strong>remote MCP</strong>, or <strong>MCP server URL</strong>.</p><h3 id="_2-add-the-qelos-server" tabindex="-1">2. Add the Qelos server <a class="header-anchor" href="#_2-add-the-qelos-server" aria-label="Permalink to “2. Add the Qelos server”">​</a></h3><ol><li>Open your Claude product&#39;s connector or MCP settings.</li><li>Add a <strong>remote MCP</strong> or <strong>custom connector</strong>.</li><li>Enter the server URL:</li></ol><div dir="ltr"><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark" style="${ssrRenderStyle({ "--shiki-light": "#24292e", "--shiki-dark": "#e1e4e8", "--shiki-light-bg": "#fff", "--shiki-dark-bg": "#24292e" })}" tabindex="0" dir="ltr"><code><span class="line"><span>https://&lt;your-tenant-domain&gt;/api/mcp/admin</span></span></code></pre></div></div><ol start="4"><li>Confirm the redirect URI matches what you added in the admin panel.</li></ol><h3 id="_3-authenticate-1" tabindex="-1">3. Authenticate <a class="header-anchor" href="#_3-authenticate-1" aria-label="Permalink to “3. Authenticate”">​</a></h3><ol><li>Save the connector and start a connection (or use a feature that calls MCP tools).</li><li>Claude opens a browser window to your tenant consent screen.</li><li>Sign in and click <strong>Accept</strong>.</li><li>The browser redirects to the Claude callback URL; the client stores the session and lists available tools.</li></ol><h2 id="codex-chatgpt" tabindex="-1">Codex / ChatGPT <a class="header-anchor" href="#codex-chatgpt" aria-label="Permalink to “Codex / ChatGPT”">​</a></h2><p>OpenAI&#39;s ChatGPT and Codex surfaces support remote MCP connectors with OAuth.</p><h3 id="_1-permit-the-callback-url-2" tabindex="-1">1. Permit the callback URL <a class="header-anchor" href="#_1-permit-the-callback-url-2" aria-label="Permalink to “1. Permit the callback URL”">​</a></h3><p>In <strong>MCP Configuration</strong>, add:</p><div dir="ltr"><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark" style="${ssrRenderStyle({ "--shiki-light": "#24292e", "--shiki-dark": "#e1e4e8", "--shiki-light-bg": "#fff", "--shiki-dark-bg": "#24292e" })}" tabindex="0" dir="ltr"><code><span class="line"><span>https://chatgpt.com/aip/mcp/oauth/callback</span></span></code></pre></div></div><p>URLs on <code>chatgpt.com</code> or <code>*.openai.com</code> match the admin <strong>Codex / ChatGPT</strong> hint. Use the exact redirect URI from your connector configuration.</p><h3 id="_2-add-the-qelos-server-1" tabindex="-1">2. Add the Qelos server <a class="header-anchor" href="#_2-add-the-qelos-server-1" aria-label="Permalink to “2. Add the Qelos server”">​</a></h3><ol><li>Open ChatGPT <strong>Settings</strong> (or your Codex / developer MCP settings).</li><li>Find <strong>Connectors</strong>, <strong>Apps</strong>, or <strong>MCP servers</strong> — add a <strong>remote MCP</strong> entry.</li><li>Enter:</li></ol><div dir="ltr"><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark" style="${ssrRenderStyle({ "--shiki-light": "#24292e", "--shiki-dark": "#e1e4e8", "--shiki-light-bg": "#fff", "--shiki-dark-bg": "#24292e" })}" tabindex="0" dir="ltr"><code><span class="line"><span>https://&lt;your-tenant-domain&gt;/api/mcp/admin</span></span></code></pre></div></div><ol start="4"><li>Ensure the OAuth redirect URI in the connector matches your permitted list entry.</li></ol><h3 id="_3-authenticate-2" tabindex="-1">3. Authenticate <a class="header-anchor" href="#_3-authenticate-2" aria-label="Permalink to “3. Authenticate”">​</a></h3><ol><li>Enable or connect the Qelos MCP server in ChatGPT/Codex.</li><li>When prompted, complete sign-in in the browser consent screen.</li><li>Click <strong>Accept</strong> to grant access.</li><li>Return to ChatGPT — Qelos tools should be available for the session.</li></ol><h2 id="devin" tabindex="-1">Devin <a class="header-anchor" href="#devin" aria-label="Permalink to “Devin”">​</a></h2><p><a href="https://devin.ai" target="_blank" rel="noreferrer">Devin</a> connects to remote MCP servers for tenant-aware automation.</p><h3 id="_1-permit-the-callback-url-3" tabindex="-1">1. Permit the callback URL <a class="header-anchor" href="#_1-permit-the-callback-url-3" aria-label="Permalink to “1. Permit the callback URL”">​</a></h3><p>Add Devin&#39;s OAuth redirect URI:</p><div dir="ltr"><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark" style="${ssrRenderStyle({ "--shiki-light": "#24292e", "--shiki-dark": "#e1e4e8", "--shiki-light-bg": "#fff", "--shiki-dark-bg": "#24292e" })}" tabindex="0" dir="ltr"><code><span class="line"><span>https://app.devin.ai/oauth/callback</span></span></code></pre></div></div><p>Other URLs on <code>*.devin.ai</code> are recognized by the admin form. Add the exact URI Devin displays in its MCP setup.</p><h3 id="_2-add-the-qelos-server-2" tabindex="-1">2. Add the Qelos server <a class="header-anchor" href="#_2-add-the-qelos-server-2" aria-label="Permalink to “2. Add the Qelos server”">​</a></h3><ol><li>In Devin, open <strong>Settings</strong> or <strong>Integrations</strong> → <strong>MCP</strong> (or <strong>Remote MCP</strong>).</li><li>Add a new remote MCP server.</li><li>Set the URL to:</li></ol><div dir="ltr"><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark" style="${ssrRenderStyle({ "--shiki-light": "#24292e", "--shiki-dark": "#e1e4e8", "--shiki-light-bg": "#fff", "--shiki-dark-bg": "#24292e" })}" tabindex="0" dir="ltr"><code><span class="line"><span>https://&lt;your-tenant-domain&gt;/api/mcp/admin</span></span></code></pre></div></div><h3 id="_3-authenticate-3" tabindex="-1">3. Authenticate <a class="header-anchor" href="#_3-authenticate-3" aria-label="Permalink to “3. Authenticate”">​</a></h3><ol><li>Save the server configuration and initiate connection.</li><li>Devin opens a browser to your tenant&#39;s <code>/mcp/authorize</code> consent screen.</li><li>Sign in with an account that passes <a href="./configuration#admin-only-access">admin-only</a> and <a href="./tools">tool</a> checks.</li><li>Click <strong>Accept</strong> — Devin receives the OAuth callback and registers Qelos tools.</li></ol><h2 id="other-mcp-clients" tabindex="-1">Other MCP clients <a class="header-anchor" href="#other-mcp-clients" aria-label="Permalink to “Other MCP clients”">​</a></h2><p>Any client that supports remote MCP with OAuth can connect if:</p><ol><li>Its redirect URI is added to <strong>Permitted callback URLs</strong> (HTTPS or custom URI scheme — see <a href="./configuration#permitted-callback-urls">Configuration</a>).</li><li>The client is pointed at <code>https://&lt;your-tenant-domain&gt;/api/mcp/admin</code>.</li><li>The connecting user completes browser consent.</li></ol><p>For custom clients built on <code>@qelos/sdk</code>, see the SDK&#39;s MCP auth helpers — that path is for advanced integrations, not standard admin setup.</p><h2 id="troubleshooting" tabindex="-1">Troubleshooting <a class="header-anchor" href="#troubleshooting" aria-label="Permalink to “Troubleshooting”">​</a></h2><table tabindex="0"><thead><tr><th>Symptom</th><th>Likely cause</th><th>Fix</th></tr></thead><tbody><tr><td>Connection fails immediately; no consent screen</td><td>MCP server disabled</td><td>Enable <strong>Enable MCP server</strong> in <a href="./configuration">MCP Configuration</a> and save</td></tr><tr><td>OAuth error; redirect rejected</td><td>Callback URL not permitted</td><td>Add the client&#39;s <strong>exact</strong> redirect URI to permitted callback URLs</td></tr><tr><td>Consent shows <strong>Administrator access required</strong></td><td>Admin-only mode + non-admin user</td><td>Sign in as admin/privileged user, or disable admin-only and scope <a href="./tools">tools</a></td></tr><tr><td>Consent succeeds but no tools in client</td><td>No tool categories enabled for user&#39;s role</td><td>Enable categories in the Tools table; match the user&#39;s <a href="/auth/permissions-roles">role</a></td></tr><tr><td>Tool calls fail with permission errors</td><td>Role / workspace filters too narrow</td><td>Widen <strong>Roles</strong>, <strong>Workspace roles</strong>, or <strong>Workspace labels</strong> for the category — or use an account that matches</td></tr><tr><td><strong>Authorization unavailable</strong> or expired request</td><td>Stale OAuth state</td><td>Start again from the client — do not refresh the consent URL manually</td></tr><tr><td><strong>Missing authorization state</strong></td><td>Broken redirect back to client</td><td>Retry from the MCP client; confirm callback URL matches permitted list exactly</td></tr><tr><td>Works for admin, not for editors</td><td>Admin-only still enabled</td><td>Disable admin-only only after scoping tools for non-admin roles</td></tr><tr><td>Workspace-scoped tools missing</td><td>Workspaces inactive or labels don&#39;t match</td><td>Enable workspaces in tenant config; adjust wsRoles/wsLabels — see <a href="./tools#workspace-columns-when-workspaces-are-inactive">Tools</a></td></tr></tbody></table><div class="warning custom-block"><p class="custom-block-title">Check admin first</p><p>Most connection failures trace back to <a href="./configuration">Configuration</a> — MCP disabled, missing callback URL, or admin-only blocking the signing-in user. Confirm those before debugging client-side settings.</p></div><h2 id="next-steps" tabindex="-1">Next steps <a class="header-anchor" href="#next-steps" aria-label="Permalink to “Next steps”">​</a></h2><p>Once connected, try natural-language prompts against your tenant — see <a href="./examples">Examples &amp; Ideas</a>.</p><h2 id="related-documentation" tabindex="-1">Related documentation <a class="header-anchor" href="#related-documentation" aria-label="Permalink to “Related documentation”">​</a></h2><ul><li><a href="./">MCP overview</a> — prerequisites and endpoint URL</li><li><a href="./configuration">Configuration</a> — enable server, callback URLs, consent flow</li><li><a href="./tools">Permitted tools</a> — tool categories and role restrictions</li><li><a href="/auth/permissions-roles">Permissions &amp; Roles</a> — tenant and workspace roles</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("mcp/clients.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const clients = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  clients as default
};
