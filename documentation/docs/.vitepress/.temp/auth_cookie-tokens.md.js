import { resolveComponent, useSSRContext } from "vue";
import { ssrRenderAttrs, ssrRenderStyle, ssrRenderComponent } from "vue/server-renderer";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Cookie Token Lifecycle","description":"","frontmatter":{"title":"Cookie Token Lifecycle","editLink":true},"headers":[],"relativePath":"auth/cookie-tokens.md","filePath":"auth/cookie-tokens.md","lastUpdated":1778514070000}');
const _sfc_main = { name: "auth/cookie-tokens.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  const _component_CopyOrDownloadAsMarkdownButtons = resolveComponent("CopyOrDownloadAsMarkdownButtons");
  _push(`<div${ssrRenderAttrs(_attrs)}><div style="${ssrRenderStyle({ "display": "none" })}" hidden="true" aria-hidden="true">Are you an LLM? You can read better optimized documentation at /auth/cookie-tokens.md for this page in Markdown format</div><h1 id="cookie-token-lifecycle-and-refresh" tabindex="-1">Cookie Token Lifecycle and Refresh <a class="header-anchor" href="#cookie-token-lifecycle-and-refresh" aria-label="Permalink to “Cookie Token Lifecycle and Refresh”">​</a></h1>`);
  _push(ssrRenderComponent(_component_CopyOrDownloadAsMarkdownButtons, null, null, _parent));
  _push(`<p>Cookie sessions are the default mode for browser-facing apps and the Qelos integrators. This page covers what is in the cookie, how it gets refreshed, and how the integrator middleware handles refresh automatically.</p><h2 id="what-s-in-the-cookie" tabindex="-1">What&#39;s in the cookie <a class="header-anchor" href="#what-s-in-the-cookie" aria-label="Permalink to “What&#39;s in the cookie”">​</a></h2><p>After signin (or social-callback exchange), Qelos sets a single <code>HttpOnly</code> cookie containing a signed JWT. The cookie is named per tenant; the integrator-side default is <code>q_access_token</code>, with a paired <code>q_refresh_token</code> cookie used for transparent refresh.</p><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark" style="${ssrRenderStyle({ "--shiki-light": "#24292e", "--shiki-dark": "#e1e4e8", "--shiki-light-bg": "#fff", "--shiki-dark-bg": "#24292e" })}" tabindex="0" dir="ltr"><code><span class="line"><span>Set-Cookie: q_access_token=eyJhbGc…; HttpOnly; SameSite=Lax; Path=/</span></span>
<span class="line"><span>Set-Cookie: q_refresh_token=eyJhbGc…; HttpOnly; SameSite=Lax; Path=/</span></span></code></pre></div><p>Cookie attributes:</p><ul><li><code>HttpOnly</code> — JavaScript on the client cannot read the value.</li><li><code>SameSite=Lax</code> for same-domain deployments; <code>SameSite=None; Secure</code> is set when the auth service has a <code>cookieBaseDomain</code> (cross-subdomain setups).</li><li><code>Secure</code> — set whenever the configured <code>appUrl</code> is <code>https://</code>.</li><li><code>Path=/</code> — sent on every request to the integrator app.</li></ul><h2 id="refreshing-manually" tabindex="-1">Refreshing manually <a class="header-anchor" href="#refreshing-manually" aria-label="Permalink to “Refreshing manually”">​</a></h2><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="${ssrRenderStyle({ "--shiki-light": "#24292e", "--shiki-dark": "#e1e4e8", "--shiki-light-bg": "#fff", "--shiki-dark-bg": "#24292e" })}" tabindex="0" dir="ltr"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">const</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> result</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> await</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> sdk.authentication.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">refreshCookieToken</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">();</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// result.payload.cookieToken      — the new cookie value (server-set on response)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// result.payload.user</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// result.payload.workspace</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// result.headers[&#39;set-cookie&#39;]    — the Set-Cookie header to relay to the browser</span></span></code></pre></div><p><code>refreshCookieToken()</code> calls <code>POST /api/cookie/refresh</code>. If the SDK is running in a browser, the cookie is replayed automatically and the new cookie is set by the response. If you are running server-side and need to authenticate the refresh request explicitly, pass the existing cookie token:</p><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="${ssrRenderStyle({ "--shiki-light": "#24292e", "--shiki-dark": "#e1e4e8", "--shiki-light-bg": "#fff", "--shiki-dark-bg": "#24292e" })}" tabindex="0" dir="ltr"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">await</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> sdk.authentication.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">refreshCookieToken</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">(currentCookieToken);</span></span></code></pre></div><p>Most application code never calls this directly — session updates from Qelos often arrive as <code>Set-Cookie</code> on <code>/api/me</code> or SDK responses when using an integrator (see below).</p><h2 id="automatic-identity-and-cookies-in-integrator-middleware" tabindex="-1">Automatic identity and cookies in integrator middleware <a class="header-anchor" href="#automatic-identity-and-cookies-in-integrator-middleware" aria-label="Permalink to “Automatic identity and cookies in integrator middleware”">​</a></h2><p>Server-side integrators (Express, Next.js, Nuxt, Fastify, NestJS, FastAPI) identify the user by calling the managed Qelos app’s <strong><code>GET /api/me</code></strong> through the resolved proxy target (same origin as your BFF). They forward the inbound <strong><code>Cookie</code></strong> and <strong><code>Authorization</code></strong> headers, then forward any upstream <strong><code>Set-Cookie</code></strong> headers from that response with the <strong><code>Domain=</code></strong> attribute rewritten to your app’s host so the browser keeps a first-party session.</p><p>They then create a <strong>request-scoped SDK</strong> whose <code>extraHeaders</code> hook forwards the current cookies on every SDK call — so cookie rotations applied earlier in the pipeline are visible to subsequent API calls in the same request.</p><p>This replaces older middleware patterns that wired <code>forceRefresh</code> and <code>onTokenRefresh</code> inside the integrator. For low-level SDK refresh options when you construct <code>QelosSDK</code> yourself, see <a href="./../sdk/token_refresh">Token refresh</a>.</p><h3 id="custom-persistence" tabindex="-1">Custom persistence <a class="header-anchor" href="#custom-persistence" aria-label="Permalink to “Custom persistence”">​</a></h3><p>If you <strong>do not</strong> use an integrator and instead instantiate <code>QelosSDK</code> directly, you can pass <code>onTokenRefresh</code> / cookie hooks as documented in the SDK — that path is independent from integrator middleware.</p><h3 id="concurrent-requests" tabindex="-1">Concurrent requests <a class="header-anchor" href="#concurrent-requests" aria-label="Permalink to “Concurrent requests”">​</a></h3><p>When several SDK calls hit a <code>401</code>, the SDK may coalesce refresh work (see SDK docs). Integrators rely on the shared <code>/api/me</code> probe plus per-request SDK instances rather than mutable token pairs on the context object.</p><h2 id="when-refresh-fails" tabindex="-1">When refresh fails <a class="header-anchor" href="#when-refresh-fails" aria-label="Permalink to “When refresh fails”">​</a></h2><p>A failed refresh (expired refresh token, revoked session, network error) propagates as an error. To handle it explicitly, wire <code>onFailedRefreshToken</code> when constructing the SDK directly:</p><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="${ssrRenderStyle({ "--shiki-light": "#24292e", "--shiki-dark": "#e1e4e8", "--shiki-light-bg": "#fff", "--shiki-dark-bg": "#24292e" })}" tabindex="0" dir="ltr"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">const</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> sdk</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> =</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> new</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> QelosSDK</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">({</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  appUrl: process.env.</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">QELOS_APP_URL</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">!</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">  onFailedRefreshToken</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">async</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> () </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">    // e.g. clear local state and bounce to login</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    location.href </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;/login&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">    return</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}"> null</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  },</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">});</span></span></code></pre></div><p>In integrator-managed apps you usually do not need this — the middleware lets the error bubble and your error handler decides what to do (redirect to login, return 401, etc.).</p><h2 id="logging-out" tabindex="-1">Logging out <a class="header-anchor" href="#logging-out" aria-label="Permalink to “Logging out”">​</a></h2><p><code>sdk.authentication.logout()</code> clears server-side tokens and the cookie. After logout, the next request through the middleware will see no auth material and fall through to your unauthenticated handling (<code>requireAuth: true</code> → 401, otherwise just a non-populated <code>req.qelos.user</code>).</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("auth/cookie-tokens.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const cookieTokens = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  cookieTokens as default
};
