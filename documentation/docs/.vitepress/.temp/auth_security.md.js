import { resolveComponent, useSSRContext } from "vue";
import { ssrRenderAttrs, ssrRenderStyle, ssrRenderComponent } from "vue/server-renderer";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Security Best Practices","description":"","frontmatter":{"title":"Security Best Practices","editLink":true},"headers":[],"relativePath":"auth/security.md","filePath":"auth/security.md","lastUpdated":1778156920000}');
const _sfc_main = { name: "auth/security.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  const _component_CopyOrDownloadAsMarkdownButtons = resolveComponent("CopyOrDownloadAsMarkdownButtons");
  _push(`<div${ssrRenderAttrs(_attrs)}><div style="${ssrRenderStyle({ "display": "none" })}" hidden="true" aria-hidden="true">Are you an LLM? You can read better optimized documentation at /auth/security.md for this page in Markdown format</div><h1 id="security-best-practices" tabindex="-1">Security Best Practices <a class="header-anchor" href="#security-best-practices" aria-label="Permalink to “Security Best Practices”">​</a></h1>`);
  _push(ssrRenderComponent(_component_CopyOrDownloadAsMarkdownButtons, null, null, _parent));
  _push(`<p>This page collects the security guidance for integrators using Qelos auth: how to store tokens, how cookies should be configured, and how to defend against the usual suspects (CSRF, XSS, leaked refresh tokens).</p><h2 id="token-storage" tabindex="-1">Token storage <a class="header-anchor" href="#token-storage" aria-label="Permalink to “Token storage”">​</a></h2><table tabindex="0"><thead><tr><th>Token</th><th>Where</th><th>Why</th></tr></thead><tbody><tr><td>Cookie session token</td><td><code>HttpOnly</code> cookie set by the server</td><td>Inaccessible to JS — XSS cannot exfiltrate it.</td></tr><tr><td>OAuth access token</td><td>In-memory only (SDK holds it)</td><td>Short-lived; never persist to <code>localStorage</code>.</td></tr><tr><td>OAuth refresh token</td><td>Secure storage (server session, OS keychain on native)</td><td>Long-lived; never persist to <code>localStorage</code> in a browser.</td></tr><tr><td>API token</td><td>Secret manager / env var</td><td>Never commit; never embed in client bundles.</td></tr></tbody></table><h3 id="don-t-put-any-token-in-localstorage" tabindex="-1">Don&#39;t put any token in <code>localStorage</code> <a class="header-anchor" href="#don-t-put-any-token-in-localstorage" aria-label="Permalink to “Don&#39;t put any token in localStorage”">​</a></h3><p><code>localStorage</code> is readable by any script on the origin. A single XSS payload can drain every token you put there. Stick to:</p><ul><li><strong>Browser apps</strong> → cookie sessions (default for the integrators).</li><li><strong>Native apps</strong> → platform keychain APIs.</li><li><strong>CLI / scripts</strong> → env vars or OS-level secret stores.</li></ul><h3 id="don-t-put-refresh-tokens-in-urls" tabindex="-1">Don&#39;t put refresh tokens in URLs <a class="header-anchor" href="#don-t-put-refresh-tokens-in-urls" aria-label="Permalink to “Don&#39;t put refresh tokens in URLs”">​</a></h3><p>The social-auth callback uses an <code>?rt=…</code> parameter exactly once and the client immediately exchanges it. After exchange:</p><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="${ssrRenderStyle({ "--shiki-light": "#24292e", "--shiki-dark": "#e1e4e8", "--shiki-light-bg": "#fff", "--shiki-dark-bg": "#24292e" })}" tabindex="0" dir="ltr"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">history.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">replaceState</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">({}, </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, location.pathname);</span></span></code></pre></div><p>Strip the parameter so it does not appear in browser history, analytics events, or the <code>Referer</code> header on subsequent navigations.</p><h2 id="cookie-settings" tabindex="-1">Cookie settings <a class="header-anchor" href="#cookie-settings" aria-label="Permalink to “Cookie settings”">​</a></h2><p>The Qelos auth service issues cookies with:</p><ul><li><code>HttpOnly</code> — always.</li><li><code>Secure</code> — when the auth service is reached over HTTPS or when a <code>cookieBaseDomain</code> is configured.</li><li><code>SameSite=Lax</code> — same-domain deployments (cookie + app on the same registrable domain).</li><li><code>SameSite=None; Secure</code> — cross-subdomain deployments (e.g. cookies set on <code>.example.com</code> and used by <code>app.example.com</code> and <code>admin.example.com</code>).</li><li><code>Path=/api</code> (auth service) and <code>Path=/</code> (integrator-rewritten cookies).</li></ul><h3 id="when-to-use-samesite-lax-vs-none" tabindex="-1">When to use <code>SameSite=Lax</code> vs <code>None</code> <a class="header-anchor" href="#when-to-use-samesite-lax-vs-none" aria-label="Permalink to “When to use SameSite=Lax vs None”">​</a></h3><ul><li>Single-domain SaaS where browser, app, and API all live under the same registrable domain → <code>Lax</code> is enough. The browser will not send the cookie on cross-origin POSTs, blocking the simplest CSRF patterns.</li><li>Cross-subdomain or split frontend/backend domains → <code>None</code> + <code>Secure</code> is required. You must add explicit CSRF protection (see below).</li></ul><h2 id="csrf" tabindex="-1">CSRF <a class="header-anchor" href="#csrf" aria-label="Permalink to “CSRF”">​</a></h2><p>CSRF risk depends on the auth mode:</p><table tabindex="0"><thead><tr><th>Mode</th><th>CSRF risk</th><th>Mitigation</th></tr></thead><tbody><tr><td>Cookie + <code>SameSite=Lax</code></td><td>Low for state-changing GETs/POSTs from third-party origins</td><td>The <code>SameSite</code> attribute itself is the primary defense; pair it with safe HTTP methods (don&#39;t mutate on <code>GET</code>).</td></tr><tr><td>Cookie + <code>SameSite=None</code></td><td>Yes — any origin can attempt cross-site submission</td><td>Add a CSRF token (double-submit cookie or synchronizer token) on every state-changing route.</td></tr><tr><td>OAuth (bearer header)</td><td>None — browsers do not auto-attach <code>Authorization</code> headers</td><td>n/a</td></tr><tr><td>API token (<code>x-api-key</code>)</td><td>None — same reason</td><td>n/a</td></tr></tbody></table><p>For the OAuth social flow, the <code>state</code> parameter is your CSRF guard: generate a random value before redirecting, persist it client-side, and reject the callback if it does not echo back. See <a href="./social-auth#step-1-build-the-login-url">Social authentication</a>.</p><h2 id="xss-and-content-security-policy" tabindex="-1">XSS and Content Security Policy <a class="header-anchor" href="#xss-and-content-security-policy" aria-label="Permalink to “XSS and Content Security Policy”">​</a></h2><p>Cookie-based sessions tolerate XSS poorly even though the cookie is <code>HttpOnly</code> — an attacker can still ride the session by issuing fetch/XHR calls from the victim&#39;s page. Defenses:</p><ul><li>Ship a strict Content Security Policy (<code>script-src &#39;self&#39;</code>, no inline scripts where possible).</li><li>Sanitize user-rendered HTML — never <code>v-html</code> / <code>dangerouslySetInnerHTML</code> with untrusted input.</li><li>Avoid <code>eval</code>, <code>new Function(...)</code>, and string-based timer callbacks.</li><li>Pin third-party scripts via Subresource Integrity (SRI).</li></ul><h2 id="api-tokens" tabindex="-1">API tokens <a class="header-anchor" href="#api-tokens" aria-label="Permalink to “API tokens”">​</a></h2><p>Specific to long-lived API tokens:</p><ol><li><strong>Never commit tokens.</strong> Store in env vars, GitHub Actions secrets, or a secret manager (Vault, AWS Secrets Manager, GCP Secret Manager).</li><li><strong>Set a real expiration.</strong> 90 days is a reasonable upper bound for most CI tokens. Avoid multi-year tokens.</li><li><strong>Scope to a workspace</strong> when the consumer only needs one workspace. A workspace-scoped token cannot be tricked into touching another workspace.</li><li><strong>One token per consumer.</strong> Use a distinctive <code>nickname</code> (&quot;<code>GitHub Actions — staging</code>&quot;, &quot;<code>Plugin XYZ — prod</code>&quot;) so you can identify and revoke a single consumer without affecting others.</li><li><strong>Rotate on suspicion.</strong> Create the new token, deploy it, then revoke the old one. There is no &quot;rotate in place&quot; — see <a href="./api-tokens#rotating-tokens">Rotating tokens</a>.</li><li><strong>Monitor <code>lastUsedAt</code>.</strong> The List endpoint exposes last-used timestamps. Tokens that are unused for 30+ days are usually safe to revoke.</li><li><strong>Self-management is blocked.</strong> API tokens cannot create, list, or delete tokens. This caps the blast radius of a leaked token to the data the token was scoped to.</li></ol><h2 id="logging-and-incident-response" tabindex="-1">Logging and incident response <a class="header-anchor" href="#logging-and-incident-response" aria-label="Permalink to “Logging and incident response”">​</a></h2><ul><li><strong>Do not log tokens.</strong> Most loggers will helpfully serialize request headers — strip <code>Authorization</code>, <code>Cookie</code>, and <code>x-api-key</code> before logging.</li><li><strong>Audit token use.</strong> The auth service writes events for token creation, deletion, and authentication failures. Wire these to your SIEM / events log.</li><li><strong>On a leak</strong>: revoke the affected token (<code>deleteApiToken</code>), then rotate any tokens that may have been created or used during the exposure window. Cookie sessions can be invalidated by changing the user&#39;s password or via the auth service&#39;s session-revocation tools.</li></ul><h2 id="quick-checklist" tabindex="-1">Quick checklist <a class="header-anchor" href="#quick-checklist" aria-label="Permalink to “Quick checklist”">​</a></h2><p>Before going live with an integrator:</p><ul><li>[ ] Cookies served over HTTPS in production</li><li>[ ] <code>cookieBaseDomain</code> set correctly for cross-subdomain deployments, with <code>SameSite=None; Secure</code></li><li>[ ] CSRF token middleware mounted on state-changing routes when using <code>SameSite=None</code></li><li>[ ] OAuth <code>state</code> parameter validated on social-auth callback</li><li>[ ] No tokens in <code>localStorage</code> / <code>sessionStorage</code></li><li>[ ] No <code>rt=</code> parameter left in browser URL after social callback</li><li>[ ] API tokens stored in secret manager, not committed</li><li>[ ] API token expirations set to ≤ 90 days where practical</li><li>[ ] Logging redacts auth headers</li><li>[ ] Strict CSP and HTML sanitization on user-rendered content</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("auth/security.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const security = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  security as default
};
