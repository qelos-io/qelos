import { resolveComponent, useSSRContext } from "vue";
import { ssrRenderAttrs, ssrRenderStyle, ssrRenderComponent } from "vue/server-renderer";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"MCP Configuration","description":"","frontmatter":{"title":"MCP Configuration","editLink":true},"headers":[],"relativePath":"mcp/configuration.md","filePath":"mcp/configuration.md","lastUpdated":1785182463000}');
const _sfc_main = { name: "mcp/configuration.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  const _component_CopyOrDownloadAsMarkdownButtons = resolveComponent("CopyOrDownloadAsMarkdownButtons");
  _push(`<div${ssrRenderAttrs(_attrs)}><div style="${ssrRenderStyle({ "display": "none" })}" hidden="true" aria-hidden="true">Are you an LLM? You can read better optimized documentation at /mcp/configuration.md for this page in Markdown format</div><h1 id="mcp-configuration" tabindex="-1">MCP Configuration <a class="header-anchor" href="#mcp-configuration" aria-label="Permalink to “MCP Configuration”">​</a></h1>`);
  _push(ssrRenderComponent(_component_CopyOrDownloadAsMarkdownButtons, null, null, _parent));
  _push(`<p>Use <strong>MCP Configuration</strong> in the admin panel to enable Qelos as a remote MCP server for your tenant, control who can connect, and permit OAuth callback URLs for supported AI clients.</p><p>The configuration is stored under the <code>mcp-configuration</code> key. Only privileged administrators can access it from <strong>Privileged Home</strong>.</p><h2 id="open-mcp-configuration" tabindex="-1">Open MCP Configuration <a class="header-anchor" href="#open-mcp-configuration" aria-label="Permalink to “Open MCP Configuration”">​</a></h2><ol><li>Sign in to the admin panel with a privileged account.</li><li>Go to <strong>Privileged Home</strong> (the admin-only home tab).</li><li>Click <strong>MCP Configuration</strong> on the configuration cards grid.</li></ol><p>The form has three sections: <strong>General</strong> (server settings), <strong>OAuth</strong> (permitted callback URLs), and <strong>Tools</strong> (exposed tool categories). Tool permissions are covered in <a href="./tools">Tools</a>.</p><div class="tip custom-block"><p class="custom-block-title">Default posture</p><p>MCP ships <strong>disabled</strong>, with <strong>admin-only access</strong> enabled and all tools off. Enable the server and callback URLs first; turn on tools deliberately after you know what each client needs.</p></div><h2 id="general-settings" tabindex="-1">General settings <a class="header-anchor" href="#general-settings" aria-label="Permalink to “General settings”">​</a></h2><h3 id="enable-mcp-server" tabindex="-1">Enable MCP server <a class="header-anchor" href="#enable-mcp-server" aria-label="Permalink to “Enable MCP server”">​</a></h3><table tabindex="0"><thead><tr><th>Setting</th><th>Field</th><th>Default</th></tr></thead><tbody><tr><td><strong>Enable MCP server</strong></td><td><code>enabled</code></td><td><code>false</code></td></tr></tbody></table><p>Turn this on to expose the tenant MCP endpoint at:</p><div dir="ltr"><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark" style="${ssrRenderStyle({ "--shiki-light": "#24292e", "--shiki-dark": "#e1e4e8", "--shiki-light-bg": "#fff", "--shiki-dark-bg": "#24292e" })}" tabindex="0" dir="ltr"><code><span class="line"><span>https://&lt;your-tenant-domain&gt;/api/mcp/admin</span></span></code></pre></div></div><p>While disabled, remote clients cannot connect and the admin form shows a warning banner. OAuth callback URLs and tool toggles remain editable but have no effect until the server is enabled.</p><h3 id="admin-only-access" tabindex="-1">Admin-only access <a class="header-anchor" href="#admin-only-access" aria-label="Permalink to “Admin-only access”">​</a></h3><table tabindex="0"><thead><tr><th>Setting</th><th>Field</th><th>Default</th></tr></thead><tbody><tr><td><strong>Admin-only access</strong></td><td><code>adminOnly</code></td><td><code>true</code></td></tr></tbody></table><p>When enabled, only admin or privileged users can complete the OAuth consent flow. Non-admins who attempt to connect see an <strong>Administrator access required</strong> message on the consent screen and cannot grant access.</p><p>Use this as your starting posture. Disable it only when you intentionally want non-admin roles to connect MCP clients — and ensure <a href="./tools">tool permissions</a> are scoped accordingly.</p><h3 id="server-name-and-version" tabindex="-1">Server name and version <a class="header-anchor" href="#server-name-and-version" aria-label="Permalink to “Server name and version”">​</a></h3><table tabindex="0"><thead><tr><th>Setting</th><th>Field</th><th>Required</th></tr></thead><tbody><tr><td><strong>Server name</strong></td><td><code>serverName</code></td><td>No</td></tr><tr><td><strong>Server version</strong></td><td><code>serverVersion</code></td><td>No</td></tr></tbody></table><p>Optional metadata sent to MCP clients during discovery. If left blank, defaults are used. Example values:</p><div dir="ltr"><div class="language-"><button title="Copy Code" class="copy"></button><span class="lang"></span><pre class="shiki shiki-themes github-light github-dark" style="${ssrRenderStyle({ "--shiki-light": "#24292e", "--shiki-dark": "#e1e4e8", "--shiki-light-bg": "#fff", "--shiki-dark-bg": "#24292e" })}" tabindex="0" dir="ltr"><code><span class="line"><span>Server name:    qelos-mcp</span></span>
<span class="line"><span>Server version: 1.0.0</span></span></code></pre></div></div><p>These fields do not affect authorization or which tools are exposed.</p><h2 id="permitted-callback-urls" tabindex="-1">Permitted callback URLs <a class="header-anchor" href="#permitted-callback-urls" aria-label="Permalink to “Permitted callback URLs”">​</a></h2><p>OAuth redirect URIs from MCP clients must appear on the <strong>Permitted callback URLs</strong> list. Only listed URLs are accepted when a client authenticates against your tenant.</p><p>When MCP is enabled, <strong>at least one callback URL is required</strong>. Add URLs with <strong>Add callback URL</strong>; remove rows you no longer need.</p><h3 id="known-client-patterns" tabindex="-1">Known client patterns <a class="header-anchor" href="#known-client-patterns" aria-label="Permalink to “Known client patterns”">​</a></h3><p>The admin form shows common patterns. Add the exact redirect URI your client uses — it must match character for character.</p><table tabindex="0"><thead><tr><th>Client</th><th>Example callback URL</th></tr></thead><tbody><tr><td><strong>Cursor</strong></td><td><code>cursor://anysphere.cursor-mcp/oauth/callback</code></td></tr><tr><td><strong>Claude</strong> (Claude.ai, Anthropic domains)</td><td><code>https://claude.ai/api/mcp/auth_callback</code></td></tr><tr><td><strong>Codex / ChatGPT</strong> (OpenAI domains)</td><td><code>https://chatgpt.com/aip/mcp/oauth/callback</code></td></tr><tr><td><strong>Devin</strong></td><td><code>https://app.devin.ai/oauth/callback</code></td></tr><tr><td><strong>Generic HTTPS</strong></td><td><code>https://localhost:6274/oauth/callback</code></td></tr><tr><td><strong>Custom URI scheme</strong></td><td><code>my-app://oauth/callback</code></td></tr></tbody></table><p>Claude and Codex/ChatGPT also accept other URLs on their respective domains (for example <code>*.anthropic.com</code> or <code>*.openai.com</code>). Devin accepts URLs on <code>*.devin.ai</code>. When you enter a URL, the form may show a <strong>Matches</strong> hint if it recognizes the client pattern.</p><div class="warning custom-block"><p class="custom-block-title">Add before connecting</p><p>If a client’s redirect URI is not on this list, authentication fails even when MCP is enabled and the user is an admin. Add the URI here before following <a href="./clients">client setup steps</a>.</p></div><h3 id="validation-rules" tabindex="-1">Validation rules <a class="header-anchor" href="#validation-rules" aria-label="Permalink to “Validation rules”">​</a></h3><p>The admin form validates callback URLs on save:</p><table tabindex="0"><thead><tr><th>Rule</th><th>Error</th></tr></thead><tbody><tr><td>MCP enabled with zero URLs</td><td>At least one callback URL is required when MCP is enabled</td></tr><tr><td>Duplicate entries</td><td>Duplicate callback URLs are not allowed</td></tr><tr><td>Empty row</td><td>Callback URL is required (per row)</td></tr><tr><td>Invalid format</td><td>Invalid callback URL format</td></tr></tbody></table><p><strong>Valid formats:</strong></p><ul><li><strong>HTTPS URLs</strong> — must parse as a valid URL (for example <code>https://example.com/oauth/callback</code>).</li><li><strong>Custom URI schemes</strong> — must match <code>scheme://…</code> where the scheme starts with a letter and uses allowed characters (for example <code>cursor://…</code>, <code>my-app://oauth/callback</code>).</li></ul><p>URLs are trimmed before save; empty rows are dropped.</p><h2 id="oauth-consent-flow" tabindex="-1">OAuth consent flow <a class="header-anchor" href="#oauth-consent-flow" aria-label="Permalink to “OAuth consent flow”">​</a></h2><p>When a user connects an MCP client, the client opens a browser window to your tenant’s authorization page at <strong><code>/mcp/authorize</code></strong>.</p><h3 id="what-the-user-sees" tabindex="-1">What the user sees <a class="header-anchor" href="#what-the-user-sees" aria-label="Permalink to “What the user sees”">​</a></h3><ol><li><strong>Sign in</strong> — if not already authenticated, the user is redirected to login and returned to the consent screen.</li><li><strong>Consent details</strong> — the screen shows: <ul><li><strong>Client</strong> — recognized name when possible (Cursor, Claude, Codex / ChatGPT, Devin) or a formatted callback target</li><li><strong>Callback URL</strong> — the OAuth redirect URI the client requested</li><li><strong>Client ID</strong> — when provided by the client</li><li><strong>Requested scopes</strong> — when the client requests scopes</li></ul></li><li><strong>Accept or Deny</strong> — the user grants or refuses access.</li></ol><p>On <strong>Accept</strong>, the browser redirects back to the client’s callback URL and the MCP session can proceed. On <strong>Deny</strong>, the client receives an access-denied result.</p><p>No manual token copy-paste is required for standard client setups — the browser handles consent and redirect.</p><h3 id="when-consent-is-blocked" tabindex="-1">When consent is blocked <a class="header-anchor" href="#when-consent-is-blocked" aria-label="Permalink to “When consent is blocked”">​</a></h3><table tabindex="0"><thead><tr><th>Condition</th><th>What the user sees</th></tr></thead><tbody><tr><td>MCP disabled</td><td>MCP is disabled — remote clients cannot connect</td></tr><tr><td>Admin-only + non-admin user</td><td>Administrator access required</td></tr><tr><td>Invalid or expired request</td><td>Authorization unavailable — start again from the MCP client</td></tr><tr><td>Missing authorization state</td><td>Missing authorization state — start again from the MCP client</td></tr></tbody></table><p>Fix configuration in this panel (enable MCP, add callback URL, adjust admin-only) and retry from the client.</p><h2 id="save-configuration" tabindex="-1">Save configuration <a class="header-anchor" href="#save-configuration" aria-label="Permalink to “Save configuration”">​</a></h2><p>Click <strong>Save</strong> at the bottom of the form. Validation runs on callback URLs before the configuration is persisted.</p><p>After saving:</p><ul><li>Confirm <strong>Enabled</strong> and <strong>Admin only</strong> tags on the form header match your intent.</li><li>Add client-specific callback URLs before users connect — see <a href="./clients">Clients</a>.</li><li>Enable and scope tool categories — see <a href="./tools">Tools</a>.</li></ul><h2 id="related-documentation" tabindex="-1">Related documentation <a class="header-anchor" href="#related-documentation" aria-label="Permalink to “Related documentation”">​</a></h2><ul><li><a href="./">MCP overview</a> — prerequisites and endpoint URL</li><li><a href="./tools">Tools</a> — exposed tool categories and role restrictions</li><li><a href="./clients">Clients</a> — connect Cursor, Claude, ChatGPT, Devin, and others</li><li><a href="/auth/permissions-roles">Permissions &amp; Roles</a> — how Qelos roles map to access</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("mcp/configuration.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const configuration = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  configuration as default
};
