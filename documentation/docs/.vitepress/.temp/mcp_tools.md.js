import { resolveComponent, useSSRContext } from "vue";
import { ssrRenderAttrs, ssrRenderStyle, ssrRenderComponent } from "vue/server-renderer";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"MCP Permitted Tools","description":"","frontmatter":{"title":"MCP Permitted Tools","editLink":true},"headers":[],"relativePath":"mcp/tools.md","filePath":"mcp/tools.md","lastUpdated":1785184244000}');
const _sfc_main = { name: "mcp/tools.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  const _component_CopyOrDownloadAsMarkdownButtons = resolveComponent("CopyOrDownloadAsMarkdownButtons");
  _push(`<div${ssrRenderAttrs(_attrs)}><div style="${ssrRenderStyle({ "display": "none" })}" hidden="true" aria-hidden="true">Are you an LLM? You can read better optimized documentation at /mcp/tools.md for this page in Markdown format</div><h1 id="permitted-mcp-tools" tabindex="-1">Permitted MCP Tools <a class="header-anchor" href="#permitted-mcp-tools" aria-label="Permalink to “Permitted MCP Tools”">​</a></h1>`);
  _push(ssrRenderComponent(_component_CopyOrDownloadAsMarkdownButtons, null, null, _parent));
  _push(`<p>The <strong>Tools</strong> section of <a href="./configuration">MCP Configuration</a> controls which tool categories are exposed on your tenant MCP server and who can use them. Each row is a <strong>category</strong> (for example <code>blueprints</code>), not an individual MCP tool name — all registered tools in that category inherit the same exposure settings.</p><div class="tip custom-block"><p class="custom-block-title">Default posture</p><p>Every category ships <strong>disabled</strong> with empty role filters. Enable categories deliberately and scope them by role before connecting clients. Pair with <strong>admin-only access</strong> until you understand what each user needs.</p></div><h2 id="open-the-tools-table" tabindex="-1">Open the tools table <a class="header-anchor" href="#open-the-tools-table" aria-label="Permalink to “Open the tools table”">​</a></h2><ol><li>Go to <strong>Privileged Home → MCP Configuration</strong>.</li><li>Scroll to the <strong>Tools</strong> panel (<strong>Exposed MCP tools</strong>).</li><li>For each category row, set <strong>Enabled</strong>, <strong>Roles</strong>, <strong>Workspace roles</strong>, and <strong>Workspace labels</strong> as needed.</li><li>Click <strong>Save</strong>.</li></ol><p>The table lists eleven categories. The <strong>Tool</strong> column shows the admin label and the category ID (for example <code>blueprints</code>).</p><h2 id="per-category-controls" tabindex="-1">Per-category controls <a class="header-anchor" href="#per-category-controls" aria-label="Permalink to “Per-category controls”">​</a></h2><table tabindex="0"><thead><tr><th>Column</th><th>Field</th><th>Behavior</th></tr></thead><tbody><tr><td><strong>Enabled</strong></td><td><code>enabled</code></td><td>When off, no tools in this category are available to any user, regardless of role.</td></tr><tr><td><strong>Roles</strong></td><td><code>roles</code></td><td>Tenant-level roles allowed to use tools in this category. Select <strong>All roles</strong> (<code>*</code>) or specific roles (for example <code>admin</code>, <code>editor</code>). <strong>Empty</strong> means no role restriction — any authenticated user who passes other checks can use the category.</td></tr><tr><td><strong>Workspace roles</strong></td><td><code>wsRoles</code></td><td>Workspace membership roles required when the user acts in a workspace context. Use <strong>All workspace roles</strong> (<code>*</code>) or specific values. <strong>Empty</strong> means no workspace-role restriction.</td></tr><tr><td><strong>Workspace labels</strong></td><td><code>wsLabels</code></td><td>User&#39;s active workspace must carry at least one of these labels. <strong>Empty</strong> means no label restriction. When a label is set, the user must match at least one listed label.</td></tr></tbody></table><h3 id="workspace-columns-when-workspaces-are-inactive" tabindex="-1">Workspace columns when workspaces are inactive <a class="header-anchor" href="#workspace-columns-when-workspaces-are-inactive" aria-label="Permalink to “Workspace columns when workspaces are inactive”">​</a></h3><p><strong>Workspace roles</strong> and <strong>Workspace labels</strong> are disabled in the admin form when workspace configuration is not active (<code>workspace-configuration.isActive</code> is <code>false</code>). In that mode, only <strong>Enabled</strong> and <strong>Roles</strong> apply.</p><p>When you later enable workspaces, configure workspace scoping before opening MCP to non-admin roles.</p><h3 id="category-id-vs-individual-tool-names" tabindex="-1">Category ID vs individual tool names <a class="header-anchor" href="#category-id-vs-individual-tool-names" aria-label="Permalink to “Category ID vs individual tool names”">​</a></h3><p>Admin stores configuration by <strong>category ID</strong> (<code>blueprints</code>, <code>users</code>, <code>events</code>, …). The MCP server resolves each registered tool against its category:</p><ol><li>Look for an <code>exposedTools</code> entry whose <code>toolId</code> matches the tool&#39;s own ID (for example <code>list-blueprints</code>).</li><li>If none, fall back to an entry whose <code>toolId</code> matches the tool&#39;s <strong>category</strong> (for example <code>blueprints</code>).</li></ol><p>In practice, configure the category row — all tools in that category share the same enabled flag and role filters.</p><h2 id="how-authorization-works" tabindex="-1">How authorization works <a class="header-anchor" href="#how-authorization-works" aria-label="Permalink to “How authorization works”">​</a></h2><p>When a connected client lists or calls tools, Qelos evaluates checks in order:</p><div class="language-mermaid"><button title="Copy Code" class="copy"></button><span class="lang">mermaid</span><pre class="shiki shiki-themes github-light github-dark" style="${ssrRenderStyle({ "--shiki-light": "#24292e", "--shiki-dark": "#e1e4e8", "--shiki-light-bg": "#fff", "--shiki-dark-bg": "#24292e" })}" tabindex="0" dir="ltr"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">flowchart TD</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  A[User requests MCP tool] --&gt; B{Admin-only mode?}</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  B --&gt;|Yes| C{Privileged user?}</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  B --&gt;|No| D{Category enabled?}</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  C --&gt;|No| X[Denied]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  C --&gt;|Yes| D</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  D --&gt;|No| X</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  D --&gt;|Yes| E{Roles match?}</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  E --&gt;|No| X</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  E --&gt;|Yes| F{Workspace roles match?}</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  F --&gt;|No| X</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  F --&gt;|Yes| G{Workspace labels match?}</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  G --&gt;|No| X</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  G --&gt;|Yes| H{Tool requires admin privilege?}</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  H --&gt;|Yes| I{Privileged user?}</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  H --&gt;|No| Y[Allowed]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  I --&gt;|No| X</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  I --&gt;|Yes| Y</span></span></code></pre></div><h3 id="admin-only-gate" tabindex="-1">Admin-only gate <a class="header-anchor" href="#admin-only-gate" aria-label="Permalink to “Admin-only gate”">​</a></h3><p>When <a href="./configuration#admin-only-access">admin-only access</a> is enabled, only <strong>privileged</strong> users can use MCP at all — users with the <code>admin</code> role or the privileged flag. Non-privileged users are blocked before any tool check runs.</p><h3 id="category-and-role-filters" tabindex="-1">Category and role filters <a class="header-anchor" href="#category-and-role-filters" aria-label="Permalink to “Category and role filters”">​</a></h3><p>For each tool, the server loads the category&#39;s <code>exposedTools</code> config and verifies:</p><ul><li><strong><code>enabled</code></strong> is <code>true</code></li><li><strong><code>roles</code></strong> — if non-empty, the user must have at least one listed role, or the list must include <code>*</code></li><li><strong><code>wsRoles</code></strong> — same rule for workspace roles</li><li><strong><code>wsLabels</code></strong> — if non-empty, the user&#39;s workspace must include at least one listed label</li></ul><h3 id="admin-privileged-tools" tabindex="-1">Admin-privileged tools <a class="header-anchor" href="#admin-privileged-tools" aria-label="Permalink to “Admin-privileged tools”">​</a></h3><p>Some registered tools require <strong>admin privilege</strong> in addition to passing category filters (<code>requiredPrivilege: &#39;admin&#39;</code>). Even if the category is enabled for a non-admin role, these tools remain unavailable unless the connecting user is privileged.</p><p>Tools marked <strong>Admin only</strong> in the table below always require a privileged user, regardless of role filters on the category row.</p><h2 id="tool-categories" tabindex="-1">Tool categories <a class="header-anchor" href="#tool-categories" aria-label="Permalink to “Tool categories”">​</a></h2><p>The admin table defines eleven categories. The <strong>Registered tools</strong> column lists MCP tools currently in the server registry for that category.</p><table tabindex="0"><thead><tr><th>Category ID</th><th>Admin label</th><th>Description</th><th>Registered tools</th></tr></thead><tbody><tr><td><code>blueprints</code></td><td>Blueprints</td><td>List and query blueprint data models</td><td><code>list-blueprints</code>, <code>get-blueprint-entities</code>, <code>create-blueprint-entity</code></td></tr><tr><td><code>users</code></td><td>Users</td><td>Manage tenant users and profiles</td><td><code>list-users</code> (<strong>Admin only</strong>)</td></tr><tr><td><code>configurations</code></td><td>Configurations</td><td>Read and update tenant configurations</td><td><code>get-app-config</code>, <code>list-configurations</code> (<strong>Admin only</strong>)</td></tr><tr><td><code>workspaces</code></td><td>Workspaces</td><td>Manage workspaces, members, and labels</td><td><code>list-workspaces</code></td></tr><tr><td><code>events</code></td><td>Events</td><td>List, emit, and subscribe to application events</td><td><code>list-events</code> (<strong>Admin only</strong>)</td></tr><tr><td><code>plugins</code></td><td>Plugins</td><td>Manage installed plugins and micro-frontends</td><td>—</td></tr><tr><td><code>agents</code></td><td>AI agents</td><td>Chat with and configure AI agents</td><td>—</td></tr><tr><td><code>integrations</code></td><td>Integrations</td><td>Manage integration sources and workflows</td><td><code>list-integration-sources</code> (<strong>Admin only</strong>)</td></tr><tr><td><code>permissions</code></td><td>Permissions</td><td>Check and manage user permissions</td><td>—</td></tr><tr><td><code>content</code></td><td>Content</td><td>Manage drafts and published content</td><td>—</td></tr><tr><td><code>storage</code></td><td>Storage</td><td>Manage storage buckets and uploaded assets</td><td>—</td></tr></tbody></table><p>Categories without registered tools appear in the admin table so you can pre-configure exposure before new tools ship. Enabling those rows has no effect until tools are added to the registry.</p><h3 id="blueprints-blueprints" tabindex="-1">Blueprints (<code>blueprints</code>) <a class="header-anchor" href="#blueprints-blueprints" aria-label="Permalink to “Blueprints (blueprints)”">​</a></h3><p>Expose data-model operations to AI clients:</p><table tabindex="0"><thead><tr><th>Tool</th><th>What it does</th></tr></thead><tbody><tr><td><code>list-blueprints</code></td><td>List blueprint definitions for the tenant</td></tr><tr><td><code>get-blueprint-entities</code></td><td>List entities for a blueprint (supports <code>limit</code>, <code>skip</code>, <code>sort</code>)</td></tr><tr><td><code>create-blueprint-entity</code></td><td>Create a new entity in a blueprint</td></tr></tbody></table><p>All three use the standard user SDK path. See <a href="/getting-started/create-blueprints">Create Blueprints</a> for blueprint concepts.</p><h3 id="configurations-configurations" tabindex="-1">Configurations (<code>configurations</code>) <a class="header-anchor" href="#configurations-configurations" aria-label="Permalink to “Configurations (configurations)”">​</a></h3><table tabindex="0"><thead><tr><th>Tool</th><th>What it does</th><th>Privilege</th></tr></thead><tbody><tr><td><code>get-app-config</code></td><td>Fetch tenant app configuration metadata</td><td>User</td></tr><tr><td><code>list-configurations</code></td><td>List tenant configuration documents</td><td>Admin</td></tr></tbody></table><h3 id="workspaces-workspaces" tabindex="-1">Workspaces (<code>workspaces</code>) <a class="header-anchor" href="#workspaces-workspaces" aria-label="Permalink to “Workspaces (workspaces)”">​</a></h3><table tabindex="0"><thead><tr><th>Tool</th><th>What it does</th></tr></thead><tbody><tr><td><code>list-workspaces</code></td><td>List workspaces available to the authenticated user</td></tr></tbody></table><p>Use <strong>Workspace roles</strong> and <strong>Workspace labels</strong> on this category (and others) to limit exposure when workspaces are active.</p><h3 id="users-events-integrations" tabindex="-1">Users, events, integrations <a class="header-anchor" href="#users-events-integrations" aria-label="Permalink to “Users, events, integrations”">​</a></h3><p>These categories expose admin-only registry tools:</p><table tabindex="0"><thead><tr><th>Category</th><th>Tool</th><th>What it does</th></tr></thead><tbody><tr><td><code>users</code></td><td><code>list-users</code></td><td>List tenant users (optional <code>username</code>, <code>roles</code> filters)</td></tr><tr><td><code>events</code></td><td><code>list-events</code></td><td>List tenant events (supports pagination and filters)</td></tr><tr><td><code>integrations</code></td><td><code>list-integration-sources</code></td><td>List integration sources (optional <code>kind</code> filter)</td></tr></tbody></table><p>Privileged users still need the category <strong>Enabled</strong> and must pass role/workspace filters.</p><h3 id="agents-plugins-content-storage-permissions" tabindex="-1">Agents, plugins, content, storage, permissions <a class="header-anchor" href="#agents-plugins-content-storage-permissions" aria-label="Permalink to “Agents, plugins, content, storage, permissions”">​</a></h3><p>These categories are defined in the admin UI for future and extended tooling. No MCP tools are registered for them yet. Leave them disabled unless you are testing upcoming capabilities.</p><h2 id="recommended-starting-configurations" tabindex="-1">Recommended starting configurations <a class="header-anchor" href="#recommended-starting-configurations" aria-label="Permalink to “Recommended starting configurations”">​</a></h2><p>Start minimal, verify client connectivity, then expand.</p><h3 id="read-only-administrator" tabindex="-1">Read-only administrator <a class="header-anchor" href="#read-only-administrator" aria-label="Permalink to “Read-only administrator”">​</a></h3><p>Best first setup while learning MCP:</p><table tabindex="0"><thead><tr><th>Setting</th><th>Value</th></tr></thead><tbody><tr><td><strong>Admin-only access</strong></td><td>Enabled</td></tr><tr><td><strong>blueprints</strong></td><td>Enabled, roles: <code>admin</code></td></tr><tr><td><strong>configurations</strong></td><td>Enabled, roles: <code>admin</code></td></tr></tbody></table><p>Connected admins can inspect blueprints and app configuration. <code>list-configurations</code> works because admins are privileged; <code>get-app-config</code> is available to any role that passes filters, but admin-only mode limits who can connect.</p><p>Example prompts once connected: <em>&quot;List all blueprints&quot;</em> or <em>&quot;What is the current app configuration?&quot;</em></p><h3 id="broader-team-—-blueprint-read-access" tabindex="-1">Broader team — blueprint read access <a class="header-anchor" href="#broader-team-—-blueprint-read-access" aria-label="Permalink to “Broader team — blueprint read access”">​</a></h3><p>When you disable admin-only and want editors to query data without admin tools:</p><table tabindex="0"><thead><tr><th>Setting</th><th>Value</th></tr></thead><tbody><tr><td><strong>Admin-only access</strong></td><td>Disabled</td></tr><tr><td><strong>blueprints</strong></td><td>Enabled, roles: <code>editor</code>, wsRoles: <code>member</code> (adjust to your workspace roles)</td></tr></tbody></table><p>Editors can call <code>list-blueprints</code> and <code>get-blueprint-entities</code> but not <code>create-blueprint-entity</code> unless your <a href="/auth/permissions-roles">blueprint permissions</a> allow entity creation through the SDK path. Keep <strong>users</strong>, <strong>events</strong>, and <strong>integrations</strong> disabled unless editors need them.</p><h3 id="incident-triage-—-events-for-admins" tabindex="-1">Incident triage — events for admins <a class="header-anchor" href="#incident-triage-—-events-for-admins" aria-label="Permalink to “Incident triage — events for admins”">​</a></h3><p>For operational debugging with admin users:</p><table tabindex="0"><thead><tr><th>Setting</th><th>Value</th></tr></thead><tbody><tr><td><strong>Admin-only access</strong></td><td>Enabled</td></tr><tr><td><strong>events</strong></td><td>Enabled, roles: <code>admin</code></td></tr><tr><td><strong>blueprints</strong></td><td>Enabled, roles: <code>admin</code></td></tr></tbody></table><p>Admins can pull recent error events and cross-reference blueprint data. See <a href="./examples">Examples &amp; Ideas</a> for prompt patterns.</p><h2 id="security-notes" tabindex="-1">Security notes <a class="header-anchor" href="#security-notes" aria-label="Permalink to “Security notes”">​</a></h2><ul><li><strong>Enable admin-only first</strong> — prevents non-admins from completing OAuth consent while you configure tools.</li><li><strong>Enable the smallest set of categories</strong> — each enabled category exposes every registered tool in that category to users who pass filters.</li><li><strong>Avoid <code>*</code> on roles</strong> unless every tenant role should access the category — prefer explicit roles such as <code>admin</code> or <code>editor</code>.</li><li><strong>Admin-privileged tools</strong> (<code>list-users</code>, <code>list-events</code>, etc.) still require a privileged connecting user even when role filters are broad.</li><li><strong>Review after role changes</strong> — if you add new tenant or workspace roles, revisit MCP tool rows to ensure exposure still matches intent.</li></ul><h2 id="related-documentation" tabindex="-1">Related documentation <a class="header-anchor" href="#related-documentation" aria-label="Permalink to “Related documentation”">​</a></h2><ul><li><a href="./configuration">MCP Configuration</a> — enable the server, callback URLs, admin-only mode</li><li><a href="./clients">Clients</a> — connect Cursor, Claude, ChatGPT, Devin</li><li><a href="./examples">Examples &amp; Ideas</a> — prompts and workflows once tools are permitted</li><li><a href="/auth/permissions-roles">Permissions &amp; Roles</a> — tenant and workspace role model</li><li><a href="/ai/agents">AI Agents</a> — agent configuration (future <code>agents</code> category tools)</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("mcp/tools.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const tools = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  tools as default
};
