import { resolveComponent, useSSRContext } from "vue";
import { ssrRenderAttrs, ssrRenderStyle, ssrRenderComponent } from "vue/server-renderer";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Authentication","description":"","frontmatter":{"title":"Authentication"},"headers":[],"relativePath":"plugin-play/authentication.md","filePath":"plugin-play/authentication.md","lastUpdated":1771333671000}');
const _sfc_main = { name: "plugin-play/authentication.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  const _component_CopyOrDownloadAsMarkdownButtons = resolveComponent("CopyOrDownloadAsMarkdownButtons");
  _push(`<div${ssrRenderAttrs(_attrs)}><div style="${ssrRenderStyle({ "display": "none" })}" hidden="true" aria-hidden="true">Are you an LLM? You can read better optimized documentation at /plugin-play/authentication.md for this page in Markdown format</div><h1 id="authentication" tabindex="-1">Authentication <a class="header-anchor" href="#authentication" aria-label="Permalink to “Authentication”">​</a></h1>`);
  _push(ssrRenderComponent(_component_CopyOrDownloadAsMarkdownButtons, null, null, _parent));
  _push(`<p>Plugin Play authenticates with your Qelos instance to access APIs on behalf of a configured user. There are two supported authentication methods.</p><h2 id="api-token-authentication-recommended" tabindex="-1">API Token Authentication (Recommended) <a class="header-anchor" href="#api-token-authentication-recommended" aria-label="Permalink to “API Token Authentication (Recommended)”">​</a></h2><p>API tokens provide the simplest and most reliable authentication for plugins. A single environment variable replaces the need for username/password credentials.</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="${ssrRenderStyle({ "--shiki-light": "#24292e", "--shiki-dark": "#e1e4e8", "--shiki-light-bg": "#fff", "--shiki-dark-bg": "#24292e" })}" tabindex="0" dir="ltr"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">QELOS_URL</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">https://your-app.qelos.app</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">QELOS_API_TOKEN</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">ql_your_api_token_here</span></span></code></pre></div><p>When configured, Plugin Play:</p><ol><li>Calls <code>sdk.authentication.apiTokenSignin(apiToken)</code> on startup</li><li>Sends the <code>x-api-key</code> header on every subsequent request</li><li>Skips all token refresh logic — the API token is permanent until expiration or revocation</li></ol><h3 id="creating-a-token-for-your-plugin" tabindex="-1">Creating a Token for Your Plugin <a class="header-anchor" href="#creating-a-token-for-your-plugin" aria-label="Permalink to “Creating a Token for Your Plugin”">​</a></h3><ol><li>Log in to your Qelos admin UI</li><li>Navigate to <strong>Profile → API Tokens</strong></li><li>Click <strong>Create Token</strong></li><li>Set a descriptive nickname (e.g., &quot;My Plugin - Production&quot;)</li><li>Optionally scope it to a specific workspace</li><li>Set an expiration date</li><li>Copy the token — it is shown <strong>only once</strong></li></ol><h3 id="benefits-over-username-password" tabindex="-1">Benefits Over Username/Password <a class="header-anchor" href="#benefits-over-username-password" aria-label="Permalink to “Benefits Over Username/Password”">​</a></h3><ul><li><strong>No refresh overhead</strong> — eliminates the OAuth refresh cycle entirely</li><li><strong>Single credential</strong> — one environment variable instead of two</li><li><strong>Workspace-scoped</strong> — bind the token to a specific workspace at creation</li><li><strong>Revocable</strong> — revoke access instantly without changing passwords</li><li><strong>Auditable</strong> — each token tracks its last usage time</li></ul><h2 id="username-password-authentication-legacy" tabindex="-1">Username / Password Authentication (Legacy) <a class="header-anchor" href="#username-password-authentication-legacy" aria-label="Permalink to “Username / Password Authentication (Legacy)”">​</a></h2><p>The legacy method uses OAuth signin with automatic token refresh:</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="${ssrRenderStyle({ "--shiki-light": "#24292e", "--shiki-dark": "#e1e4e8", "--shiki-light-bg": "#fff", "--shiki-dark-bg": "#24292e" })}" tabindex="0" dir="ltr"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">QELOS_URL</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">https://your-app.qelos.app</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">QELOS_USERNAME</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">admin@company.com</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">QELOS_PASSWORD</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">secret</span></span></code></pre></div><p>Plugin Play calls <code>sdk.authentication.oAuthSignin()</code> and handles token refresh automatically via <code>onFailedRefreshToken</code>.</p><p>This method is still supported but <strong>API tokens are recommended</strong> for new deployments.</p><h2 id="authentication-priority" tabindex="-1">Authentication Priority <a class="header-anchor" href="#authentication-priority" aria-label="Permalink to “Authentication Priority”">​</a></h2><p>If both <code>QELOS_API_TOKEN</code> and <code>QELOS_USERNAME</code>/<code>QELOS_PASSWORD</code> are set, the API token takes priority.</p><h2 id="troubleshooting" tabindex="-1">Troubleshooting <a class="header-anchor" href="#troubleshooting" aria-label="Permalink to “Troubleshooting”">​</a></h2><ul><li><strong>401 errors</strong> — verify the token has not expired or been revoked</li><li><strong>403 errors</strong> — ensure the token&#39;s user has the required roles and permissions</li><li><strong>Workspace mismatch</strong> — if the token is scoped to a workspace, all operations are bound to that workspace</li></ul><p>See <a href="/plugin-play/configuration">Configuration</a> for the full list of environment variables.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("plugin-play/authentication.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const authentication = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  authentication as default
};
