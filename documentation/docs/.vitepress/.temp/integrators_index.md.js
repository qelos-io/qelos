import { resolveComponent, useSSRContext } from "vue";
import { ssrRenderAttrs, ssrRenderStyle, ssrRenderComponent } from "vue/server-renderer";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Integrators","description":"","frontmatter":{"title":"Integrators","editLink":true},"headers":[],"relativePath":"integrators/index.md","filePath":"integrators/index.md","lastUpdated":1784211253000}');
const _sfc_main = { name: "integrators/index.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  const _component_CopyOrDownloadAsMarkdownButtons = resolveComponent("CopyOrDownloadAsMarkdownButtons");
  _push(`<div${ssrRenderAttrs(_attrs)}><div style="${ssrRenderStyle({ "display": "none" })}" hidden="true" aria-hidden="true">Are you an LLM? You can read better optimized documentation at /integrators.md for this page in Markdown format</div><h1 id="integrators" tabindex="-1">Integrators <a class="header-anchor" href="#integrators" aria-label="Permalink to “Integrators”">​</a></h1>`);
  _push(ssrRenderComponent(_component_CopyOrDownloadAsMarkdownButtons, null, null, _parent));
  _push(`<p>Qelos integrators are thin per-framework packages that plug your application into a Qelos instance. They all expose the <strong>same contract</strong> — a <code>QelosRequestContext</code> with <code>{ user, workspace, workspaces, sdk }</code> — so once you&#39;ve learned one, the others are immediate.</p><p>If you haven&#39;t already, start with <a href="./../getting-started/integrators">Getting Started as an Integrator</a> for the overall flow (CLI, blueprints, deployment). The pages below are the <strong>per-framework setup guides</strong>.</p><h2 id="available-integrators" tabindex="-1">Available integrators <a class="header-anchor" href="#available-integrators" aria-label="Permalink to “Available integrators”">​</a></h2><table tabindex="0"><thead><tr><th>Framework</th><th>Package</th><th>Guide</th></tr></thead><tbody><tr><td><strong>Express</strong></td><td><code>@qelos/integrator-express</code></td><td><a href="./express">Express setup</a></td></tr><tr><td><strong>Next.js</strong> (App + Pages Router)</td><td><code>@qelos/integrator-next</code></td><td><a href="./next">Next.js setup</a></td></tr><tr><td><strong>Nuxt 3</strong></td><td><code>@qelos/integrator-nuxt</code></td><td><a href="./nuxt">Nuxt setup</a></td></tr><tr><td><strong>Fastify</strong></td><td><code>@qelos/integrator-fastify</code></td><td><a href="./fastify">Fastify setup</a></td></tr><tr><td><strong>NestJS</strong></td><td><code>@qelos/integrator-nest</code></td><td><a href="./nest">NestJS setup</a></td></tr><tr><td><strong>FastAPI</strong> (Python)</td><td><code>qelos-integrator-fastapi</code></td><td><a href="./fastapi">FastAPI setup</a></td></tr></tbody></table><p>Every guide covers the same six steps:</p><ol><li><strong>Install</strong> the integrator and the SDK.</li><li><strong>Configure</strong> the middleware / module.</li><li><strong>Access</strong> the user and workspace from your routes.</li><li><strong>Handle</strong> authentication — login, social auth, token refresh.</li><li><strong>Query</strong> entities through the request-scoped SDK.</li><li><strong>Common patterns and gotchas.</strong></li></ol><p>Hitting an error during setup? See <strong><a href="./troubleshooting">Integrator Troubleshooting</a></strong> for frequent issues (e.g. <code>no website for host: &lt;IP&gt;</code>, missing env vars, cookie / BFF misconfiguration) — especially for Next.js and Nuxt.</p><h2 id="what-every-integrator-does" tabindex="-1">What every integrator does <a class="header-anchor" href="#what-every-integrator-does" aria-label="Permalink to “What every integrator does”">​</a></h2><p>Independent of framework, all integrators run the same pipeline before your handler:</p><ol><li>Resolve the managed Qelos origin (proxy target): framework-specific env overrides, then <code>appUrl</code> / <code>app_url</code>.</li><li>Build a per-request <code>QelosSDK</code> that forwards the inbound <code>Cookie</code> and <code>Authorization</code> headers on every call (<code>extra_headers</code> / <code>extraHeaders</code>), unless <code>apiToken</code> / <code>api_token</code> is set.</li><li>When a proxy target exists, call <code>GET {origin}/api/me</code> with those headers and forward upstream <code>Set-Cookie</code> with <code>Domain=</code> rewritten to the inbound host (same-origin BFF).</li><li>Populate <code>user</code> from the <code>/api/me</code> JSON on success; load <code>workspaces</code> via <code>sdk.workspaces.getList()</code>.</li><li>Set <code>workspace</code> from <code>user.workspace</code> when present, or from an optional <code>resolveWorkspace</code> callback — <strong>not</strong> from <code>workspaces[0]</code>.</li><li>Attach the result on the framework-specific request object (<code>req.qelos</code>, <code>request.qelos</code>, <code>event.context.qelos</code>, <code>request.state.qelos</code>, or forwarded headers in Next.js edge middleware).</li></ol><div class="warning custom-block"><p class="custom-block-title custom-block-title-default">WARNING</p><p>The integrator packages are for <strong>external apps only</strong>. Apps inside the Qelos monorepo MUST NOT depend on <code>@qelos/integrator-*</code> — they talk to the gateway directly.</p></div><h2 id="shared-configuration-shape" tabindex="-1">Shared configuration shape <a class="header-anchor" href="#shared-configuration-shape" aria-label="Permalink to “Shared configuration shape”">​</a></h2><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="${ssrRenderStyle({ "--shiki-light": "#24292e", "--shiki-dark": "#e1e4e8", "--shiki-light-bg": "#fff", "--shiki-dark-bg": "#24292e" })}" tabindex="0" dir="ltr"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">{</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">  appUrl</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: string,                 </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// required — Qelos backend base URL</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  apiToken</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?:</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> string,              </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// service-to-service token (skips cookie forwarding)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  requireAuth</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?:</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> boolean,          </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// 401 on anonymous (default false)</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  skipPaths</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?:</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> string[],           </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// path prefixes to bypass entirely</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  disableProxy</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?:</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> boolean,         </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// disable catch-all /api proxy where applicable</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  sdkOptions</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">?:</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> Partial</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&lt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">QelosSDKOptions</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">&gt;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">,</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">}</span></span></code></pre></div><p>The Python integrator uses snake_case for the same fields (<code>app_url</code>, <code>api_token</code>, <code>require_auth</code>, <code>skip_paths</code>, <code>disable_proxy</code>, <code>sdk_options</code>).</p><h2 id="picking-the-right-one" tabindex="-1">Picking the right one <a class="header-anchor" href="#picking-the-right-one" aria-label="Permalink to “Picking the right one”">​</a></h2><ul><li><strong>Building a Node API or BFF?</strong> Use the integrator that matches your framework directly — Express, Fastify, or NestJS for a clean fit; Next.js or Nuxt when the same app also serves your frontend.</li><li><strong>Building a Python service?</strong> Use FastAPI; the middleware is ASGI-level so it also works in any Starlette app.</li><li><strong>Embedding inside a frontend?</strong> For browser-only data calls, use <a href="./../web-sdk/"><code>@qelos/web-sdk</code></a> instead of an integrator — the integrators are server-side only.</li><li><strong>Integrating into a framework that&#39;s not listed?</strong> Open an issue or port the contract — the Express implementation (<code>integrators/express/src</code>) is the simplest reference.</li></ul></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("integrators/index.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const index = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  index as default
};
