import { resolveComponent, useSSRContext } from "vue";
import { ssrRenderAttrs, ssrRenderStyle, ssrRenderComponent } from "vue/server-renderer";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"API Proxy","description":"","frontmatter":{"title":"API Proxy","editLink":true},"headers":[],"relativePath":"plugins/api-proxy.md","filePath":"plugins/api-proxy.md","lastUpdated":1783603990000}');
const _sfc_main = { name: "plugins/api-proxy.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  const _component_CopyOrDownloadAsMarkdownButtons = resolveComponent("CopyOrDownloadAsMarkdownButtons");
  _push(`<div${ssrRenderAttrs(_attrs)}><div style="${ssrRenderStyle({ "display": "none" })}" hidden="true" aria-hidden="true">Are you an LLM? You can read better optimized documentation at /plugins/api-proxy.md for this page in Markdown format</div><h1 id="api-proxy" tabindex="-1">API Proxy <a class="header-anchor" href="#api-proxy" aria-label="Permalink to “API Proxy”">​</a></h1>`);
  _push(ssrRenderComponent(_component_CopyOrDownloadAsMarkdownButtons, null, null, _parent));
  _push(`<p>When you host a Qelos frontend (e.g. admin or a plugin UI) on <strong>Netlify</strong>, the browser makes requests to your Netlify domain. If the frontend calls the Qelos API on another origin (e.g. <code>https://api.yourqelos.com</code>), you run into <strong>CORS</strong> and often <strong>cookie/same-origin</strong> constraints. A simple way to avoid that is to have the frontend call <strong>the same origin</strong> and let the server proxy those requests to the real Qelos API.</p><p>The <strong>@qelos/plugin-netlify-api</strong> Netlify build plugin configures your site so that <code>/api/*</code> is proxied to your Qelos API. <strong>By default</strong> it deploys the <strong>bundled serverless function</strong> <code>qelos-api-proxy</code> and rewrites <code>/api/*</code> to <code>/.netlify/functions/qelos-api-proxy</code>, so request headers (e.g. public <code>Host</code>) and <code>bypass_admin</code> are applied in Node. Optionally, <strong><code>use_cdn_proxy = true</code></strong> uses <strong>Netlify’s CDN rewrite</strong> straight to <code>\${QELOS_API_IP}/api/:splat</code> (no function — see <a href="https://docs.netlify.com/manage/routing/redirects/rewrites-proxies/#proxy-to-another-service" target="_blank" rel="noreferrer">Netlify proxy docs</a>). No CORS or cross-origin setup is required; the browser only talks to your Netlify site.</p><h2 id="how-it-works" tabindex="-1">How it works <a class="header-anchor" href="#how-it-works" aria-label="Permalink to “How it works”">​</a></h2><ol><li><p><strong>Build time (Netlify build)</strong><br> The plugin runs in the <code>onPreBuild</code> phase and:</p><ul><li>Sets <strong><code>QELOS_API_IP</code></strong> (and proxy mode). You can override via plugin inputs or Netlify env.</li><li>Adds a <strong>rewrite</strong> for <code>/api/*</code>: by default <strong><code>/.netlify/functions/qelos-api-proxy</code></strong>, or <strong><code>\${origin}/api/:splat</code></strong> if <strong><code>use_cdn_proxy</code></strong> is enabled.</li><li><strong>Prepends</strong> the same rule to <strong><code>dist/_redirects</code></strong> via <strong><code>postbuild</code> → <code>qelos-netlify-patch-redirects</code></strong>. Netlify evaluates <code>_redirects</code> <strong>before</strong> <code>netlify.toml</code>; static frameworks (e.g. Nuxt <code>netlify-static</code>) add a catch‑all <code>/*</code> rule, so the API line must be <strong>first</strong> in that file—hence the post-build patch.</li><li><strong>Unless CDN mode:</strong> copies <strong><code>qelos-api-proxy.ts</code></strong> into your Netlify functions directory.</li></ul></li><li><p><strong>Runtime</strong><br> When a user hits <code>https://your-site.netlify.app/api/...</code>:</p><ul><li><strong>Default (function):</strong> Netlify invokes <code>qelos-api-proxy</code>, which forwards to <code>QELOS_API_IP</code> using the request URL and public host handling.</li><li><strong>CDN mode:</strong> Netlify’s edge proxies the request to your Qelos API host with path <code>/api/...</code> (no <code>qelos-api-proxy.ts</code> execution).</li></ul></li></ol><h2 id="setup" tabindex="-1">Setup <a class="header-anchor" href="#setup" aria-label="Permalink to “Setup”">​</a></h2><h3 id="_1-add-the-plugin-in-netlify-toml" tabindex="-1">1. Add the plugin in <code>netlify.toml</code> <a class="header-anchor" href="#_1-add-the-plugin-in-netlify-toml" aria-label="Permalink to “1. Add the plugin in netlify.toml”">​</a></h3><p>In the repo of your frontend (e.g. Qelos admin or a plugin app) that you deploy to Netlify, add:</p><div class="language-toml"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark" style="${ssrRenderStyle({ "--shiki-light": "#24292e", "--shiki-dark": "#e1e4e8", "--shiki-light-bg": "#fff", "--shiki-dark-bg": "#24292e" })}" tabindex="0" dir="ltr"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">[[</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">plugins</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">]]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  package = </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;@qelos/plugin-netlify-api&quot;</span></span></code></pre></div><p>That’s enough for the default API URL (<code>http://159.203.152.168</code>).</p><h3 id="_2-optional-configure-the-api-url" tabindex="-1">2. (Optional) Configure the API URL <a class="header-anchor" href="#_2-optional-configure-the-api-url" aria-label="Permalink to “2. (Optional) Configure the API URL”">​</a></h3><p>You can set the Qelos API URL in three ways (in order of precedence):</p><p><strong>A. Plugin input in <code>netlify.toml</code></strong></p><div class="language-toml"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark" style="${ssrRenderStyle({ "--shiki-light": "#24292e", "--shiki-dark": "#e1e4e8", "--shiki-light-bg": "#fff", "--shiki-dark-bg": "#24292e" })}" tabindex="0" dir="ltr"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">[[</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">plugins</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">]]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  package = </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;@qelos/plugin-netlify-api&quot;</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  [</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">plugins</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">inputs</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    api_url = </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;https://api.yourqelos.com&quot;</span></span></code></pre></div><p><strong>B. Environment variable in Netlify</strong></p><p>In Netlify: <strong>Site settings → Environment variables</strong>, add <code>QELOS_API_IP</code> (e.g. <code>https://api.yourqelos.com</code>). You can use the same variable for build and for the function at runtime.</p><p><strong>C. Default</strong></p><p>If neither is set, the plugin uses <code>http://159.203.152.168</code>.</p><p>The proxy accepts either a full URL (<code>http://...</code> or <code>https://...</code>) or a hostname; it will use the correct host and port when forwarding.</p><p><strong>D. (Optional) Bypass admin header</strong></p><p>To have the proxy send <code>x-bypass-admin: true</code> on every request (so admin users are scoped as regular users), set:</p><div class="language-toml"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark" style="${ssrRenderStyle({ "--shiki-light": "#24292e", "--shiki-dark": "#e1e4e8", "--shiki-light-bg": "#fff", "--shiki-dark-bg": "#24292e" })}" tabindex="0" dir="ltr"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">[</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">plugins</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">inputs</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  bypass_admin = </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">true</span></span></code></pre></div><p>See <a href="#bypass-admin">Bypass admin</a> below.</p><h3 id="_3-point-your-frontend-at-api" tabindex="-1">3. Point your frontend at <code>/api</code> <a class="header-anchor" href="#_3-point-your-frontend-at-api" aria-label="Permalink to “3. Point your frontend at /api”">​</a></h3><p>In your frontend code, configure the base URL for the Qelos API to the <strong>same origin</strong> and path <code>/api</code>, e.g.:</p><ul><li><code>https://your-site.netlify.app/api</code> (production), or</li><li><code>http://localhost:8888/api</code> when using <code>netlify dev</code>.</li></ul><p>Then all requests to <code>/api/*</code> go to the proxy, which forwards them to the Qelos API.</p><h2 id="social-login-oauth" tabindex="-1">Social login (OAuth) <a class="header-anchor" href="#social-login-oauth" aria-label="Permalink to “Social login (OAuth)”">​</a></h2><p>When your frontend implements <a href="./../auth/social-auth">social authentication</a> through the proxy, the browser calls same-origin URLs like <code>/api/auth/linkedin?returnUrl=/auth/callback&amp;redirectUrl=…</code>. The proxy <strong>must</strong> forward the full query string and set <code>Host</code> / <code>x-forwarded-host</code> to your public site domain (e.g. <code>app.example.com</code>), not the Qelos upstream host.</p><h3 id="required-setup" tabindex="-1">Required setup <a class="header-anchor" href="#required-setup" aria-label="Permalink to “Required setup”">​</a></h3><ol><li><p><strong><code>@qelos/plugin-netlify-api</code> ≥ 4.1.2</strong> — preserves OAuth query params and resolves the public host correctly.</p></li><li><p><strong><code>postbuild</code> script</strong> in <code>package.json</code>:</p><div class="language-json"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark" style="${ssrRenderStyle({ "--shiki-light": "#24292e", "--shiki-dark": "#e1e4e8", "--shiki-light-bg": "#fff", "--shiki-dark-bg": "#24292e" })}" tabindex="0" dir="ltr"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">{ </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">&quot;scripts&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: { </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">&quot;postbuild&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;qelos-netlify-patch-redirects&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> } }</span></span></code></pre></div></li><li><p><strong><code>[[plugins]]</code></strong> entry in <code>netlify.toml</code> (see <a href="#setup">Setup</a> above).</p></li><li><p><strong><code>QELOS_API_IP</code></strong> in Netlify env — your Qelos gateway target.</p></li><li><p><strong>Qelos admin</strong> — add your app domain to <code>metadata.websiteUrls</code>.</p></li><li><p><strong>OAuth provider</strong> — register <code>https://&lt;your-app-domain&gt;/api/auth/&lt;provider&gt;/callback</code>.</p></li></ol><p>Use the <strong>function proxy</strong> (default). CDN-only mode does not run <code>qelos-api-proxy</code> and may not set headers needed for multi-domain OAuth.</p><h3 id="what-the-proxy-forwards" tabindex="-1">What the proxy forwards <a class="header-anchor" href="#what-the-proxy-forwards" aria-label="Permalink to “What the proxy forwards”">​</a></h3><table tabindex="0"><thead><tr><th>Request from browser</th><th>Proxy sends to Qelos</th></tr></thead><tbody><tr><td><code>GET /api/auth/linkedin?returnUrl=/auth/callback&amp;redirectUrl=https://app.example.com/api/auth/linkedin/callback&amp;state=…</code></td><td>Same path + query; <code>Host: app.example.com</code>; <code>x-forwarded-host: app.example.com</code></td></tr><tr><td><code>POST /api/auth/callback?rt=…</code></td><td>Same; session <code>Set-Cookie</code> rewritten to your domain</td></tr></tbody></table><p>If <code>redirectUrl</code> or <code>returnUrl</code> are missing in auth service logs, the proxy is not forwarding the query string — verify the plugin version and <code>postbuild</code> step. See <a href="./../auth/social-auth#common-mistakes">Social auth troubleshooting</a>.</p><h2 id="what-the-plugin-adds-equivalent-config" tabindex="-1">What the plugin adds (equivalent config) <a class="header-anchor" href="#what-the-plugin-adds-equivalent-config" aria-label="Permalink to “What the plugin adds (equivalent config)”">​</a></h2><p>If you were to configure Netlify by hand, the plugin effectively does the following.</p><p><strong>Environment (build and function runtime):</strong></p><div class="language-toml"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark" style="${ssrRenderStyle({ "--shiki-light": "#24292e", "--shiki-dark": "#e1e4e8", "--shiki-light-bg": "#fff", "--shiki-dark-bg": "#24292e" })}" tabindex="0" dir="ltr"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">[</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">build</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">.</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">environment</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  QELOS_API_IP = </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;http://159.203.152.168&quot;</span></span></code></pre></div><p><strong>Redirect (default — serverless function):</strong></p><div class="language-toml"><button title="Copy Code" class="copy"></button><span class="lang">toml</span><pre class="shiki shiki-themes github-light github-dark" style="${ssrRenderStyle({ "--shiki-light": "#24292e", "--shiki-dark": "#e1e4e8", "--shiki-light-bg": "#fff", "--shiki-dark-bg": "#24292e" })}" tabindex="0" dir="ltr"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">[[</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}">redirects</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">]]</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  from = </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;/api/*&quot;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  to = </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;/.netlify/functions/qelos-api-proxy&quot;</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  status = </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">200</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  force = </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">true</span></span></code></pre></div><p><strong>CDN mode (<code>use_cdn_proxy = true</code>):</strong> rewrite to <code>http://YOUR_QELOS_HOST/api/:splat</code> at the edge (no function).</p><h2 id="bypass-admin" tabindex="-1">Bypass admin <a class="header-anchor" href="#bypass-admin" aria-label="Permalink to “Bypass admin”">​</a></h2><p>Backend APIs accept a <strong>bypass-admin</strong> flag so that privileged users (e.g. admin) are treated as regular users for scope filtering. You can pass it in any of these ways:</p><ul><li><strong>Query:</strong> <code>?bypassAdmin=true</code></li><li><strong>Body:</strong> <code>{ &quot;bypassAdmin&quot;: true }</code> (for POST/PUT)</li><li><strong>Header:</strong> <code>x-bypass-admin: true</code> (or <code>x-bypass-admin: 1</code>)</li></ul><p>When using the Netlify plugin, you can set <code>bypass_admin = true</code> in <code>[plugins.inputs]</code> so the proxy adds the header <code>x-bypass-admin: true</code> to every request. That way the frontend does not need to send query or body; the proxy applies it globally for that deployment.</p><h2 id="summary" tabindex="-1">Summary <a class="header-anchor" href="#summary" aria-label="Permalink to “Summary”">​</a></h2><table tabindex="0"><thead><tr><th>What you do</th><th>What the plugin does</th></tr></thead><tbody><tr><td>Add <code>[[plugins]] package = &quot;@qelos/plugin-netlify-api&quot;</code> to <code>netlify.toml</code></td><td>Sets <code>QELOS_API_IP</code>, adds <code>/api/*</code> → <code>qelos-api-proxy</code> (or CDN if <code>use_cdn_proxy</code>), maintains <code>dist/_redirects</code> via <code>postbuild</code></td></tr><tr><td>Optionally set <code>api_url</code> in <code>[plugins.inputs]</code> or <code>QELOS_API_IP</code> in Netlify env</td><td>Uses that value as the proxy target</td></tr><tr><td>Optionally set <code>bypass_admin = true</code> in <code>[plugins.inputs]</code></td><td>Proxy adds <code>x-bypass-admin: true</code> to every request</td></tr><tr><td>Use base URL <code>/api</code> in your frontend</td><td>Browser calls same origin; proxy forwards to Qelos API</td></tr><tr><td>Implement social login</td><td>Ensure plugin ≥ 4.1.2, <code>postbuild</code> script, and <code>websiteUrls</code> + provider callback URLs are configured — see <a href="#social-login-oauth">Social login (OAuth)</a></td></tr></tbody></table><p>No need to add redirects or function code to your repo; the plugin handles it so your Netlify-deployed Qelos frontend can talk to the Qelos API through <code>/api</code> on the same origin.</p></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("plugins/api-proxy.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const apiProxy = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  apiProxy as default
};
