import { resolveComponent, useSSRContext } from "vue";
import { ssrRenderAttrs, ssrRenderStyle, ssrRenderComponent } from "vue/server-renderer";
import { _ as _export_sfc } from "./plugin-vue_export-helper.1tPrXgE0.js";
const __pageData = JSON.parse('{"title":"Integrator Troubleshooting","description":"","frontmatter":{"title":"Integrator Troubleshooting","editLink":true},"headers":[],"relativePath":"integrators/troubleshooting.md","filePath":"integrators/troubleshooting.md","lastUpdated":1784211253000}');
const _sfc_main = { name: "integrators/troubleshooting.md" };
function _sfc_ssrRender(_ctx, _push, _parent, _attrs, $props, $setup, $data, $options) {
  const _component_CopyOrDownloadAsMarkdownButtons = resolveComponent("CopyOrDownloadAsMarkdownButtons");
  _push(`<div${ssrRenderAttrs(_attrs)}><div style="${ssrRenderStyle({ "display": "none" })}" hidden="true" aria-hidden="true">Are you an LLM? You can read better optimized documentation at /integrators/troubleshooting.md for this page in Markdown format</div><h1 id="integrator-troubleshooting" tabindex="-1">Integrator Troubleshooting <a class="header-anchor" href="#integrator-troubleshooting" aria-label="Permalink to “Integrator Troubleshooting”">​</a></h1>`);
  _push(ssrRenderComponent(_component_CopyOrDownloadAsMarkdownButtons, null, null, _parent));
  _push(`<p>Common errors when wiring <code>@qelos/integrator-*</code> packages into an external app. This page is aimed at <strong>Next.js</strong> and <strong>Nuxt</strong> BFF setups, but the same gateway rules apply to every integrator.</p><p>If you have not installed an integrator yet, start with <a href="./../getting-started/integrators">Getting Started as an Integrator</a> and the <a href="./">per-framework guides</a>.</p><h2 id="no-website-for-host-ip-address" tabindex="-1"><code>no website for host: &lt;IP address&gt;</code> <a class="header-anchor" href="#no-website-for-host-ip-address" aria-label="Permalink to “no website for host: &lt;IP address&gt;”">​</a></h2><h3 id="symptom" tabindex="-1">Symptom <a class="header-anchor" href="#symptom" aria-label="Permalink to “Symptom”">​</a></h3><p>A call to <code>/api/me</code> (from middleware, a catch-all API proxy, or the SDK) returns <strong>400</strong> with a body like:</p><div class="language-json"><button title="Copy Code" class="copy"></button><span class="lang">json</span><pre class="shiki shiki-themes github-light github-dark" style="${ssrRenderStyle({ "--shiki-light": "#24292e", "--shiki-dark": "#e1e4e8", "--shiki-light-bg": "#fff", "--shiki-dark-bg": "#24292e" })}" tabindex="0" dir="ltr"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">{ </span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">&quot;message&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">: </span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&quot;no website for host: 159.203.152.168&quot;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> }</span></span></code></pre></div><p>The host in the message is a <strong>raw IP</strong> (or another hostname Qelos does not know), not your app&#39;s public URL.</p><h3 id="why-it-happens" tabindex="-1">Why it happens <a class="header-anchor" href="#why-it-happens" aria-label="Permalink to “Why it happens”">​</a></h3><p>The Qelos gateway resolves your <strong>tenant</strong> from the request <code>Host</code> header (see <a href="./../api/gateway-endpoint-reference">Gateway endpoint reference</a>). Every hostname your app uses must be registered for that tenant — typically in <strong>Admin → App configuration → Hostnames</strong> (<code>metadata.websiteUrls</code>).</p><p>Integrators call Qelos server-side via a <strong>proxy target</strong> URL. Resolution order (highest priority first):</p><table tabindex="0"><thead><tr><th>Integrator</th><th>Framework-specific override</th><th>Shared fallbacks</th><th>Final fallback</th></tr></thead><tbody><tr><td>Next.js</td><td><code>NEXT_QELOS_PROXY_TARGET</code></td><td><code>QELOS_IP</code>, <code>QELOS_API_IP</code></td><td><code>QELOS_APP_URL</code> / <code>appUrl</code></td></tr><tr><td>Nuxt</td><td><code>NUXT_QELOS_PROXY_TARGET</code></td><td><code>QELOS_IP</code>, <code>QELOS_API_IP</code></td><td><code>qelos.appUrl</code> / <code>NUXT_QELOS_APP_URL</code></td></tr><tr><td>Express, Fastify, NestJS</td><td><code>QELOS_PROXY_TARGET</code></td><td><code>QELOS_IP</code>, <code>QELOS_API_IP</code></td><td><code>appUrl</code></td></tr><tr><td>FastAPI</td><td>—</td><td><code>QELOS_PROXY_TARGET</code>, <code>QELOS_IP</code>, <code>QELOS_API_IP</code></td><td><code>app_url</code></td></tr></tbody></table><p>When <code>QELOS_IP</code> or <code>QELOS_API_IP</code> is set (common in local dev and Netlify templates), the integrator forwards traffic to that IP. The upstream request then carries <code>Host: 159.203.152.168</code> (or similar). Qelos has no website registered for that address, so tenant lookup fails.</p><p><code>QELOS_APP_URL</code> / <code>appUrl</code> is <strong>not</strong> a substitute for registering your app&#39;s own domain — it is the managed Qelos tenant base URL. Both must be correct:</p><ul><li><strong><code>appUrl</code></strong> — where server-side code reaches Qelos (tenant domain, e.g. <code>https://my-app.qelos.io</code>).</li><li><strong>Your app&#39;s public origin</strong> — what the browser and BFF use (e.g. <code>https://app.example.com</code>), listed in <code>websiteUrls</code>.</li></ul><h3 id="fix" tabindex="-1">Fix <a class="header-anchor" href="#fix" aria-label="Permalink to “Fix”">​</a></h3><p><strong>1. Set the tenant URL, not the IP, as <code>appUrl</code></strong></p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="${ssrRenderStyle({ "--shiki-light": "#24292e", "--shiki-dark": "#e1e4e8", "--shiki-light-bg": "#fff", "--shiki-dark-bg": "#24292e" })}" tabindex="0" dir="ltr"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># .env — use your Qelos tenant domain</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">QELOS_APP_URL</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">https://my-app.qelos.io</span></span></code></pre></div><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="${ssrRenderStyle({ "--shiki-light": "#24292e", "--shiki-dark": "#e1e4e8", "--shiki-light-bg": "#fff", "--shiki-dark-bg": "#24292e" })}" tabindex="0" dir="ltr"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// nuxt.config.ts</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">export</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> default</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> defineNuxtConfig</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">({</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  modules: [</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">&#39;@qelos/integrator-nuxt&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">],</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  qelos: {</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">    appUrl: process.env.</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">QELOS_APP_URL</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">!</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">, </span><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}">// https://my-app.qelos.io — not the raw IP</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  },</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">});</span></span></code></pre></div><p><strong>2. Treat <code>QELOS_IP</code> / <code>QELOS_API_IP</code> as connectivity overrides only</strong></p><p>Use them when the tenant domain is not reachable from your machine (e.g. pointing at a shared dev gateway). They route <strong>network</strong> traffic; they do <strong>not</strong> replace <code>appUrl</code> and they do <strong>not</strong> register a hostname with Qelos.</p><p>In production, prefer <strong><code>appUrl</code> alone</strong> and remove IP overrides from env.</p><p><strong>3. Register every hostname your app serves</strong></p><p>In the Qelos admin panel, add each origin users hit to <code>metadata.websiteUrls</code>, including:</p><ul><li>Production custom domain (<code>app.example.com</code>)</li><li>Staging / preview hosts</li><li>Local dev (<code>localhost:3000</code>, <code>127.0.0.1:3000</code>) when testing auth locally</li></ul><p>See <a href="./../auth/social-auth">Social authentication → websiteUrls</a> for the same allow-list used by OAuth callbacks.</p><p><strong>4. Prefer the tenant domain in development when possible</strong></p><p>Map the tenant hostname to the server IP in your OS hosts file and keep <code>QELOS_APP_URL</code> as the domain — no <code>QELOS_IP</code> needed:</p><div class="language-text"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark" style="${ssrRenderStyle({ "--shiki-light": "#24292e", "--shiki-dark": "#e1e4e8", "--shiki-light-bg": "#fff", "--shiki-dark-bg": "#24292e" })}" tabindex="0" dir="ltr"><code><span class="line"><span># /etc/hosts  (Windows: C:\\Windows\\System32\\drivers\\etc\\hosts)</span></span>
<span class="line"><span>203.0.113.10   my-app.qelos.io</span></span></code></pre></div><p>Then:</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="${ssrRenderStyle({ "--shiki-light": "#24292e", "--shiki-dark": "#e1e4e8", "--shiki-light-bg": "#fff", "--shiki-dark-bg": "#24292e" })}" tabindex="0" dir="ltr"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">QELOS_APP_URL</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">https://my-app.qelos.io</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#6A737D", "--shiki-dark": "#6A737D" })}"># unset QELOS_IP / QELOS_API_IP / *_PROXY_TARGET</span></span></code></pre></div><p><strong>5. Custom-domain BFF (Next / Nuxt on Netlify or similar)</strong></p><p>When the browser talks to <code>https://app.example.com/api/...</code> and Nitro/Next proxies to Qelos, the gateway must see <strong><code>app.example.com</code></strong> as the public host — not the upstream IP. Follow the same-origin proxy setup in <a href="./../plugins/api-proxy">API Proxy</a> (plugin ≥ 4.1.2, <code>postbuild</code> patch, <code>websiteUrls</code>, OAuth callback URLs).</p><h3 id="quick-checklist" tabindex="-1">Quick checklist <a class="header-anchor" href="#quick-checklist" aria-label="Permalink to “Quick checklist”">​</a></h3><table tabindex="0"><thead><tr><th>Check</th><th>Expected</th></tr></thead><tbody><tr><td><code>QELOS_APP_URL</code> / <code>qelos.appUrl</code></td><td>Tenant <strong>domain</strong> URL (<code>https://…</code>), not <code>http://159.…</code></td></tr><tr><td><code>QELOS_IP</code> / <code>QELOS_API_IP</code></td><td>Unset in production; dev-only if domain unreachable</td></tr><tr><td><code>metadata.websiteUrls</code></td><td>Includes your app&#39;s public host(s)</td></tr><tr><td>Browser / SDK <code>appUrl</code></td><td>Same origin as the page (<code>location.origin</code>) when using a BFF</td></tr></tbody></table><hr><h2 id="qelos-api-proxy-is-not-configured-503" tabindex="-1"><code>Qelos API proxy is not configured</code> (503) <a class="header-anchor" href="#qelos-api-proxy-is-not-configured-503" aria-label="Permalink to “Qelos API proxy is not configured (503)”">​</a></h2><h3 id="symptom-1" tabindex="-1">Symptom <a class="header-anchor" href="#symptom-1" aria-label="Permalink to “Symptom”">​</a></h3><p>Responses include:</p><div class="language-text"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark" style="${ssrRenderStyle({ "--shiki-light": "#24292e", "--shiki-dark": "#e1e4e8", "--shiki-light-bg": "#fff", "--shiki-dark-bg": "#24292e" })}" tabindex="0" dir="ltr"><code><span class="line"><span>[@qelos/integrator-next] Qelos API proxy is not configured. Set appUrl (or NEXT_QELOS_PROXY_TARGET / QELOS_IP / QELOS_API_IP in development).</span></span></code></pre></div><p>(or the Nuxt / Express / Nest / Fastify equivalent).</p><h3 id="fix-1" tabindex="-1">Fix <a class="header-anchor" href="#fix-1" aria-label="Permalink to “Fix”">​</a></h3><p>Set at least one proxy target:</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="${ssrRenderStyle({ "--shiki-light": "#24292e", "--shiki-dark": "#e1e4e8", "--shiki-light-bg": "#fff", "--shiki-dark-bg": "#24292e" })}" tabindex="0" dir="ltr"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">QELOS_APP_URL</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">https://my-app.qelos.io</span></span></code></pre></div><p>For Next.js default exports, <code>QELOS_APP_URL</code> is <strong>required</strong> before the middleware module loads. For Nuxt, set <code>qelos.appUrl</code> in <code>nuxt.config.ts</code> or <code>NUXT_QELOS_APP_URL</code> at runtime.</p><hr><h2 id="qelos-integrator-next-qelos-app-url-is-required" tabindex="-1"><code>@qelos/integrator-next: QELOS_APP_URL is required</code> <a class="header-anchor" href="#qelos-integrator-next-qelos-app-url-is-required" aria-label="Permalink to “@qelos/integrator-next: QELOS_APP_URL is required”">​</a></h2><h3 id="symptom-2" tabindex="-1">Symptom <a class="header-anchor" href="#symptom-2" aria-label="Permalink to “Symptom”">​</a></h3><p>Build or first request throws because the default middleware cannot read config.</p><h3 id="fix-2" tabindex="-1">Fix <a class="header-anchor" href="#fix-2" aria-label="Permalink to “Fix”">​</a></h3><p>Add to <code>.env.local</code> (Next.js) or your deployment env:</p><div class="language-bash"><button title="Copy Code" class="copy"></button><span class="lang">bash</span><pre class="shiki shiki-themes github-light github-dark" style="${ssrRenderStyle({ "--shiki-light": "#24292e", "--shiki-dark": "#e1e4e8", "--shiki-light-bg": "#fff", "--shiki-dark-bg": "#24292e" })}" tabindex="0" dir="ltr"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">QELOS_APP_URL</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">=</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}">https://my-app.qelos.io</span></span></code></pre></div><p>Or bypass the default export and pass config explicitly:</p><div class="language-ts"><button title="Copy Code" class="copy"></button><span class="lang">ts</span><pre class="shiki shiki-themes github-light github-dark" style="${ssrRenderStyle({ "--shiki-light": "#24292e", "--shiki-dark": "#e1e4e8", "--shiki-light-bg": "#fff", "--shiki-dark-bg": "#24292e" })}" tabindex="0" dir="ltr"><code><span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">import</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> { createQelosMiddleware } </span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">from</span><span style="${ssrRenderStyle({ "--shiki-light": "#032F62", "--shiki-dark": "#9ECBFF" })}"> &#39;@qelos/integrator-next/middleware&#39;</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">;</span></span>
<span class="line"></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">export</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}"> default</span><span style="${ssrRenderStyle({ "--shiki-light": "#6F42C1", "--shiki-dark": "#B392F0" })}"> createQelosMiddleware</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">({</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">  config: { appUrl: process.env.</span><span style="${ssrRenderStyle({ "--shiki-light": "#005CC5", "--shiki-dark": "#79B8FF" })}">QELOS_APP_URL</span><span style="${ssrRenderStyle({ "--shiki-light": "#D73A49", "--shiki-dark": "#F97583" })}">!</span><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}"> },</span></span>
<span class="line"><span style="${ssrRenderStyle({ "--shiki-light": "#24292E", "--shiki-dark": "#E1E4E8" })}">});</span></span></code></pre></div><hr><h2 id="anonymous-user-empty-getqeloscontext-after-login" tabindex="-1">Anonymous user / empty <code>getQelosContext()</code> after login <a class="header-anchor" href="#anonymous-user-empty-getqeloscontext-after-login" aria-label="Permalink to “Anonymous user / empty getQelosContext() after login”">​</a></h2><h3 id="symptom-3" tabindex="-1">Symptom <a class="header-anchor" href="#symptom-3" aria-label="Permalink to “Symptom”">​</a></h3><p>Middleware runs but <code>user</code> is always <code>null</code>, or <code>/api/me</code> returns 401/400 while cookies look present.</p><h3 id="common-causes" tabindex="-1">Common causes <a class="header-anchor" href="#common-causes" aria-label="Permalink to “Common causes”">​</a></h3><ol><li><strong>Host / tenant mismatch</strong> — see <a href="#no-website-for-host-ip-address"><code>no website for host</code></a> above; failed tenant lookup prevents session resolution.</li><li><strong>Cookies scoped to the wrong domain</strong> — Qelos sets cookies for its tenant domain; the integrator rewrites <code>Domain=</code> on <code>Set-Cookie</code> to your inbound <code>Host</code>. If you call the Qelos URL directly from the browser (cross-origin), cookies will not attach. Use the <strong>same-origin BFF</strong> (<code>/api/**</code> proxy) or <a href="./../web-sdk/"><code>@qelos/web-sdk</code></a> with <code>credentials: &#39;include&#39;</code> against your app origin.</li><li><strong>Middleware skipped the path</strong> — Next.js auto-skips <code>/api/</code> when the catch-all proxy is enabled. Identity for API routes comes from the proxy and your route handlers, not edge <code>/api/me</code>.</li><li><strong>Missing catch-all proxy</strong> — Without <code>createQelosApiProxyHandlers</code> (Next) or the Nuxt module&#39;s built-in proxy, browser SDK calls to <code>/api/...</code> never reach Qelos.</li></ol><p>See <a href="./../auth/cookie-tokens">Cookie Token Lifecycle</a> for refresh behaviour.</p><hr><h2 id="_401-from-middleware-with-requireauth-true" tabindex="-1"><code>401</code> from middleware with <code>requireAuth: true</code> <a class="header-anchor" href="#_401-from-middleware-with-requireauth-true" aria-label="Permalink to “401 from middleware with requireAuth: true”">​</a></h2><h3 id="symptom-4" tabindex="-1">Symptom <a class="header-anchor" href="#symptom-4" aria-label="Permalink to “Symptom”">​</a></h3><p>Every page or API route returns <code>401</code> before your handler runs.</p><h3 id="fix-3" tabindex="-1">Fix <a class="header-anchor" href="#fix-3" aria-label="Permalink to “Fix”">​</a></h3><p>Integrators return <strong>401</strong>, not a login redirect. Either:</p><ul><li>Set <code>requireAuth: false</code> (default) and gate protected routes yourself, or</li><li>Ensure a valid session exists (cookies on the request), or</li><li>Use <code>apiToken</code> / <code>QELOS_API_TOKEN</code> for service-to-service mode (no end-user session).</li></ul><p>Next.js: use <code>requireQelosUser()</code> + <code>redirect(&#39;/login&#39;)</code> in server components. Nuxt: check <code>useQelos().isAuthenticated</code> or pass <code>{ requireAuth: true }</code> only on routes that should fail closed.</p><hr><h2 id="oauth-social-login-failures-on-a-custom-domain" tabindex="-1">OAuth / social login failures on a custom domain <a class="header-anchor" href="#oauth-social-login-failures-on-a-custom-domain" aria-label="Permalink to “OAuth / social login failures on a custom domain”">​</a></h2><p>Symptoms include <code>redirect_uri does not match</code>, landing on <code>/</code> instead of your callback, or <code>No website URL configured for tenant</code>.</p><p>These are usually <strong><code>websiteUrls</code></strong> or <strong><code>redirectUrl</code></strong> issues, not integrator bugs. See:</p><ul><li><a href="./../auth/social-auth">Social authentication</a></li><li><a href="./../auth/social-auth#common-mistakes">Social auth common mistakes</a></li><li><a href="./../plugins/api-proxy#social-login-oauth">API Proxy → Social login</a></li></ul><hr><h2 id="environment-variable-reference" tabindex="-1">Environment variable reference <a class="header-anchor" href="#environment-variable-reference" aria-label="Permalink to “Environment variable reference”">​</a></h2><h3 id="required-everywhere" tabindex="-1">Required everywhere <a class="header-anchor" href="#required-everywhere" aria-label="Permalink to “Required everywhere”">​</a></h3><table tabindex="0"><thead><tr><th>Variable</th><th>Purpose</th></tr></thead><tbody><tr><td><code>QELOS_APP_URL</code></td><td>Managed Qelos tenant base URL (domain, not IP)</td></tr></tbody></table><h3 id="optional-—-identity-and-routing" tabindex="-1">Optional — identity and routing <a class="header-anchor" href="#optional-—-identity-and-routing" aria-label="Permalink to “Optional — identity and routing”">​</a></h3><table tabindex="0"><thead><tr><th>Variable</th><th>Default</th><th>Purpose</th></tr></thead><tbody><tr><td><code>QELOS_API_TOKEN</code></td><td>—</td><td>Service token; skips cookie-based <code>/api/me</code></td></tr><tr><td><code>QELOS_REQUIRE_AUTH</code></td><td><code>false</code></td><td>Reject anonymous requests with 401</td></tr><tr><td><code>QELOS_SKIP_PATHS</code></td><td>—</td><td>Comma-separated path prefixes to bypass middleware</td></tr><tr><td><code>QELOS_DISABLE_PROXY</code></td><td><code>false</code></td><td>Next.js: disable auto <code>/api/</code> skip when not using BFF proxy</td></tr></tbody></table><h3 id="optional-—-dev-connectivity-overrides-use-with-care" tabindex="-1">Optional — dev connectivity overrides (use with care) <a class="header-anchor" href="#optional-—-dev-connectivity-overrides-use-with-care" aria-label="Permalink to “Optional — dev connectivity overrides (use with care)”">​</a></h3><table tabindex="0"><thead><tr><th>Variable</th><th>Used by</th></tr></thead><tbody><tr><td><code>NEXT_QELOS_PROXY_TARGET</code></td><td>Next.js</td></tr><tr><td><code>NUXT_QELOS_PROXY_TARGET</code></td><td>Nuxt</td></tr><tr><td><code>QELOS_PROXY_TARGET</code></td><td>Express, Fastify, NestJS, FastAPI</td></tr><tr><td><code>QELOS_IP</code></td><td>All integrators (shared)</td></tr><tr><td><code>QELOS_API_IP</code></td><td>All integrators (shared; Netlify plugin default target)</td></tr></tbody></table><p>Higher-priority entries in each integrator&#39;s resolution chain win over <code>appUrl</code>. Leave IP overrides unset unless you understand the <a href="#no-website-for-host-ip-address"><code>no website for host</code></a> implications.</p><hr><h2 id="still-stuck" tabindex="-1">Still stuck? <a class="header-anchor" href="#still-stuck" aria-label="Permalink to “Still stuck?”">​</a></h2><ol><li>Confirm the hostname in the error is listed in <strong>Admin → App configuration → Hostnames</strong>.</li><li>Compare your env against the <a href="./next">Next.js</a> or <a href="./nuxt">Nuxt</a> setup guide (sections <strong>Environment variables</strong> / <strong>Configure the module</strong>).</li><li>For gateway-level behaviour (tenant header, internal hosts), see <a href="./../api/gateway-endpoint-reference">Gateway endpoint reference</a>.</li><li>Ask in <a href="https://discord.gg/8D6TMdzZYJ" target="_blank" rel="noreferrer">Discord</a> or open an issue on <a href="https://github.com/qelos-io/qelos" target="_blank" rel="noreferrer">GitHub</a>.</li></ol></div>`);
}
const _sfc_setup = _sfc_main.setup;
_sfc_main.setup = (props, ctx) => {
  const ssrContext = useSSRContext();
  (ssrContext.modules || (ssrContext.modules = /* @__PURE__ */ new Set())).add("integrators/troubleshooting.md");
  return _sfc_setup ? _sfc_setup(props, ctx) : void 0;
};
const troubleshooting = /* @__PURE__ */ _export_sfc(_sfc_main, [["ssrRender", _sfc_ssrRender]]);
export {
  __pageData,
  troubleshooting as default
};
